//go:build !jsonschema

// Code generated by internal/generate. DO NOT EDIT.

package main

import (
	"fmt"
	"os"
	"path/filepath"

	"github.com/spf13/cobra"
	"github.com/tylergannon/gimble"
	"github.com/tylergannon/gimble/internal/skgo/client"
	routes "github.com/tylergannon/gimble/internal/skgo/links/onzggl3sn52xizlt"
	"github.com/tylergannon/gimble/web"
	"github.com/tylergannon/polytype"
)

// validateproductCommand is gimble run validate-product and submits the matching Form remote.
func validateproductCommand(defaults map[gimble.WorkflowRole]string) *cobra.Command {
	var valueSuiteFile string
	var productOperationModel string
	var productVisualReviewModel string
	var productTriageModel string
	var workDir, project, instanceDir, conversation string
	var follow bool
	cmd := &cobra.Command{Use: "validate-product", Short: "ValidateProduct runs user workloads, checks their screenshots, and triages findings.", Long: "Package validateproduct runs practical user testing: up to three independent\nworkloads in parallel, one screenshot review, then one synthesis/issue-triage\nturn. It is a focus group, not an exhaustive feature checklist or source review.\nTesters never inspect the implementation source of the product under test (A).\nIf A does work on a second project (B), B's source is permitted but the tester\nshould normally rely on A to do that work.\n\nSupply a JSON/YAML suite with product, guides (local user-documentation files),\noutput_dir, and one to three workloads. Each workload has name, assignment_file\n(local task/issue text and allowed actions), an existing isolated workdir, url,\nand optional foreground start and ready shell commands. Prepare/build the desired\nproduct version before invocation. Existing targets are not stopped. A start\ncommand requires ready, polled for at most 30 seconds. For a CLI-only product,\nstart a loopback terminal such as GoTTY and supply its URL. Testers may use shell\ncommands as ordinary users, including invoking Gimble to delegate work on B.\n\nThe three tester slots are explicit; unused slots do nothing. The caller assigns\nworkloads; no planner invents work or retries failures. Each tester saves ordered,\ncaptioned screenshots and reports task outcome. One follow-up on the same session\nasks for its three favorite and least favorite aspects of UX and UI separately,\nappended to user-report.md. Elapsed time covers the task, excluding this debrief.\nVideo records the browser for optional human review; agents do not analyze it.\nGemini Flash opens screenshots to check readability and claims, not to repeat the workload.\nThe final agent reads all reports, deduplicates findings against existing GitHub\nissues, uploads supporting screenshots, and opens actionable issues in the\nrequired issue_repo (owner/repository). Product defects are findings, not\nworkflow execution errors; failed agent turns remain execution errors.\n\nPrerequisites: authenticated harnesses, playwright-cli and its installed browser,\nauthenticated gh, and a configured gimble upload-artifact destination.\nplaywright_cli can override the driver's executable path. timeout defaults\nto 1h. All paths resolve from the suite file. Output is a unique\nuser-testing-* directory containing reports.json,\nper-tester user-report.md, screenshots and video.webm, visual-review.md and\nfindings.md. Elapsed time is measured by the workflow. The final command/run\nstatus includes cleanup errors; files alone do not certify run completion.\nBounded browser cleanup runs outside cancellation; hard kills cannot guarantee it.\n\nRoles: product-operation defaults to Claude Opus 5.5, product-visual-review to\nGemini Flash, and product-triage to GPT-6 Astra. Each has its model override flag.\n\nExample:\n\n\tgimble run validate-product --suite-file /abs/user-testing.yaml --instance-dir /abs/instance --project /abs/project" + "\n\nThe selected persistent instance owns this run. --project selects its admitted repository; --work-dir selects the execution directory independently. --instance-dir selects the instance state directory (or GIMBLE_INSTANCE_DIR, default .gimble). --follow waits for terminal success or failure; otherwise the run continues after this client exits. Each role flag chooses a model and optional effort. Executable lookup, PATH, and provider configuration come from the instance startup environment.", Args: cobra.NoArgs}
	cmd.Flags().StringVar(&valueSuiteFile, "suite-file", "", "SuiteFile names the JSON/YAML product, local workload assignments, and issue repository. (required)")
	_ = cmd.MarkFlagRequired("suite-file")
	cmd.Flags().StringVar(&workDir, "work-dir", "", "execution directory (default: owning project)")
	cmd.Flags().StringVar(&project, "project", ".", "repository owning this run and its observation; admitted on first start")
	instanceDefault := os.Getenv("GIMBLE_INSTANCE_DIR")
	if instanceDefault == "" {
		instanceDefault = ".gimble"
	}
	cmd.Flags().StringVar(&instanceDir, "instance-dir", instanceDefault, "selected running instance state directory (default: GIMBLE_INSTANCE_DIR or .gimble)")
	cmd.Flags().StringVar(&conversation, "conversation", "", "associate this run with a conversation in the owning project")
	cmd.Flags().BoolVar(&follow, "follow", false, "wait for the hosted run's terminal result; without this flag the run survives client exit")
	productOperationModelDefault := defaults[gimble.WorkflowRole("product-operation")]
	if productOperationModelDefault == "" {
		cmd.Flags().StringVar(&productOperationModel, "product-operation", "", "the model for role product-operation, as model or model:effort; OpenCode uses opencode/model or opencode/provider/model")
		_ = cmd.MarkFlagRequired("product-operation")
	} else {
		cmd.Flags().StringVar(&productOperationModel, "product-operation", productOperationModelDefault, "advanced override for role product-operation, as model or model:effort; OpenCode uses opencode/model or opencode/provider/model; omit this flag to use the displayed workflow default")
	}
	productVisualReviewModelDefault := defaults[gimble.WorkflowRole("product-visual-review")]
	if productVisualReviewModelDefault == "" {
		cmd.Flags().StringVar(&productVisualReviewModel, "product-visual-review", "", "the model for role product-visual-review, as model or model:effort; OpenCode uses opencode/model or opencode/provider/model")
		_ = cmd.MarkFlagRequired("product-visual-review")
	} else {
		cmd.Flags().StringVar(&productVisualReviewModel, "product-visual-review", productVisualReviewModelDefault, "advanced override for role product-visual-review, as model or model:effort; OpenCode uses opencode/model or opencode/provider/model; omit this flag to use the displayed workflow default")
	}
	productTriageModelDefault := defaults[gimble.WorkflowRole("product-triage")]
	if productTriageModelDefault == "" {
		cmd.Flags().StringVar(&productTriageModel, "product-triage", "", "the model for role product-triage, as model or model:effort; OpenCode uses opencode/model or opencode/provider/model")
		_ = cmd.MarkFlagRequired("product-triage")
	} else {
		cmd.Flags().StringVar(&productTriageModel, "product-triage", productTriageModelDefault, "advanced override for role product-triage, as model or model:effort; OpenCode uses opencode/model or opencode/provider/model; omit this flag to use the displayed workflow default")
	}
	cmd.RunE = func(cmd *cobra.Command, _ []string) error {
		project, err := filepath.Abs(project)
		if err != nil {
			return err
		}
		if workDir == "" {
			workDir = project
		}
		workDir, err = filepath.Abs(workDir)
		if err != nil {
			return err
		}
		formInput := routes.StartValidateProductInput{ProjectDir: project, WorkDir: workDir, Conversation: conversation,
			SuiteFile:               valueSuiteFile,
			RoleProductOperation:    polytype.Optional[string]{Present: cmd.Flags().Changed("product-operation"), Value: productOperationModel},
			RoleProductVisualReview: polytype.Optional[string]{Present: cmd.Flags().Changed("product-visual-review"), Value: productVisualReviewModel},
			RoleProductTriage:       polytype.Optional[string]{Present: cmd.Flags().Changed("product-triage"), Value: productTriageModel},
		}
		formClient, err := web.SelectedFormClient(cmd.Context(), instanceDir, project)
		if err != nil {
			return err
		}
		accepted, err := (client.Client{FormClient: formClient}).StartValidateProduct(cmd.Context(), formInput)
		if err != nil {
			return fmt.Errorf("start validate-product: %w", err)
		}
		fmt.Fprintln(cmd.OutOrStdout(), accepted.RunID)
		if !follow {
			return nil
		}
		result, err := web.Follow(cmd.Context(), instanceDir, project, accepted.RunID)
		if err != nil {
			return err
		}
		if result.Status != "completed" {
			return fmt.Errorf("run %s %s: %s", accepted.RunID, result.Status, result.Error)
		}
		return nil
	}
	return cmd
}
