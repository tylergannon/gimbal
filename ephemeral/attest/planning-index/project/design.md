# Retry helper

Plan a Python module exposing retry_delay(attempt: int) -> int. Follow the shared engineering retry delay policy in the local semantic index. The helper computes seconds only. No scheduler, network IO, packages, or new CLI. The module filename has not been chosen: ask the person before finalizing that choice. Do not implement during this planning task.
