The direct native continuation succeeded (exit 0) using the same Claude session `8c757efc-819a-4bd3-a4c5-77bbb997fc6c` and model `claude-fable-5-1`, via `claude --resume <id> --model claude-fable-5-1 --print --verbose --output-format stream-json --permission-mode bypassPermissions <follow-up>`. It produced the contract, codec, tests, and a final response. The follow-up also included a user design clarification, so this is evidence that the conversation could continue, not a controlled proof that Gimble caused the earlier rejection. Native stream output preserves richer events than the generic error captured by the failed Gimble runs.
