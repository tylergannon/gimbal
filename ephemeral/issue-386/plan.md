# Issue 386: Pi harness through Diffusion

Authority: read `request.md` and `issue.md` beside this plan, plus the repository's AGENTS.md and docs/definition-of-done.md. The issue predates the Gimbal rename: use current `gimbal` paths and commands. Work only in /Users/tyler/src/gimbal-issue-386, based on origin/main 7460360e.

## Definition of done
A real Gimbal workflow runs through Pi against Diffusion Router, performs useful file and shell work, and returns a valid result. The implemented adapter meets the existing HarnessAdapter contract. An independent Sonnet validator sees the requested behavior working. Stop at 90–95% when only optional polish remains; failed required behavior is still a gap.

## Steps / ordered outcomes
1. Implement the Pi session adapter following existing harness conventions. Verify/install @earendil-works/pi-coding-agent 0.87.1 in a local disposable installation if absent. Keep one RPC child per session, config/session storage owned by Gimbal, native identity, and the process across turns. Correlate responses by ID, continuously drain JSONL stdout, keep stderr separate, complete at agent_settled, and obtain the authoritative final assistant result. Surface real failures; diagnostic event gaps do not invalidate success. Project assistant/tool events and reported per-model usage. Implement queued steer; with no active Gimbal turn, return landed=false without sending to Pi; if settlement wins a pending steer, clear any orphaned queued message and report landed=false so it cannot leak into the next turn. Use a focused race test against Pi 0.87.1 to determine whether its own queue draining already handles this; do not add an OpenCode-style re-prompt loop without a demonstrated need. Implement clear_queue then abort on cancellation with bounded cleanup, idempotent own-session close, saved-session resume, and an independent full-conversation fork using verified clone/get_state semantics. For schema turns put the schema in the prompt and return extracted JSON to existing Gimbal validation/re-ask; text returns a JSON string. No new extension unless the real path establishes a need.
2. Wire explicit pi/diffusion/deepseek-4.1-flash selection through modelalias, binding, run-prompt and ordinary workflow role flags. No forced high effort on an unverified model. Configure the Router endpoint with a literal environment reference for DIFFUSION_API_KEY; preserve workdir/resource behavior and session/provider isolation. Add concise setup/help in README.md, docs-site/src/routes/docs/harnesses/+page.svx and run-prompt help. Role help is generated: update internal/generate/command.go and regenerate, never edit generated files by hand. No general harness refactor, process pool, or changes to unrelated harnesses beyond necessary shared binding. Run relevant repository build/check gates once after the integration is ready.
3. Run the built application against Router with deepseek-4.1-flash, using the supplied environment key. Exercise run-prompt and an existing ordinary workflow with a Pi role; use a small disposable target workdir. Personally see a read, an edit, a shell command and a valid result, a schema result, and a follow-up in the same native conversation. Verify required cancellation, fork parent/child follow-ups, and concurrent session isolation through the built Go integration. A missing or rejected API key is a blocking gap, not a reason to substitute fake proof. If the starting model actually cannot do the task, report that failure and qualify another Router coding model as the issue permits. Sonnet independently inspects and runs the behavior needed to establish completion.

## Validation, proportionate to the adapter
Put repeatable tests beside the code. Use the pinned real Pi subprocess against a local fake Chat Completions endpoint for its RPC/config/session behavior, with ordinary unit tests where a deliberate exit or race is easier to induce. Cover the issue's stated completion, event/usage, schema re-ask, steer race, cancellation/queue clearing, fork, close, process exit and replacement-process resume cases. Combine cases naturally; do not create a conformance framework or a fixture factory for every permutation. Keep any real-Router test opt-in and use the same adapter/runtime path; no committed standalone proof program.

Relevant gates are just vet, just test, just fmt-check and just build as applicable. Browser e2e is required only if browser behavior changes. Do not rerun passing gates just to accumulate evidence. Report actual Pi version, Router model, observed event/usage and request compatibility from the successful interaction; never capture bearer headers or raw keys. Existing runtime records and ordinary tests suffice. Do not add proof scripts, attestations, result files, dashboards, raw request capture infrastructure, or committed run output. Describe observations in the workflow response and eventual PR/chat.

## Execution
Use the existing implement workflow with these three outcomes, max 4 tasks per outcome. Planner and independent validator: claude-sonnet-5:high; coder: gpt-6-luna:high; scope supervisor: the displayed default gpt-6-astra:medium. Each outcome is a result to achieve, not an invitation to redesign the adapter architecture. The workflow does not commit/push/merge; this launch request ends once it is running detached. Do not alter the outcomes file or workflow. DIFFUSION_API_KEY is supplied by the instance startup environment; use it without printing it.

## Local runtime setup
Pi 0.87.1 is installed and verified at /Users/tyler/.local/share/gimbal-issue-386/pi/node_modules/.bin/pi. Its version-matched protocol and model documentation is local at /Users/tyler/.local/share/gimbal-issue-386/pi/node_modules/@earendil-works/pi-coding-agent/docs/ (rpc.md, rpc-commands.md, json.md, models.md, configuration.md). Read these local sources for details.
The dedicated instance has the key and Pi executable on PATH. Codex uses a shared daemon and may not inherit a new instance environment; before go test, just test, or any command needing Pi or the key, source /Users/tyler/.local/share/gimbal-issue-386/router.env with shell tracing off. This file is outside the repository; do not cat it, copy it, or print environment values. It provides the key and Pi PATH to either harness. No credential-handling feature needs to be added to Gimbal for this launch.

Use the usual bare `pi` executable from PATH, with the local install above on PATH for this run; no new binary-discovery feature is required. Unit tests run without Pi. Real-Pi package tests explicitly check `pi --version` and skip with a stated reason if absent or not 0.87.1; for this task the reviewer must see them actually run with the verified 0.87.1 binary, not count a skip as validation.
Before submission, the launching agent verified that sourcing the external environment file provides a nonempty key (without printing it) and Pi reports 0.87.1. Check key presence again before live qualification. The adapter must not log its child environment or interpolate the key into config or run records. Use the version-matched documented environment reference and test that it resolves.
Leave effort blank by default; unsupported explicit effort gets a clear error rather than silently enabling thinking. Keep session storage long enough for replacement-process resume. Fork validation runs the independent parent and child follow-ups on two native processes.

Report actual token usage. Zero or unknown cost from the minimal unpriced Router model is not a completion gap; do not invent prices.
