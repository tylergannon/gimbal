Use `kazz187/jev-sdk-go` v0.2.0 for typed Jev experiments.

Ask bounded semantic questions against small transcript snapshots, keeping deterministic checks in Go. Jev should frequently screen runs for plan conflicts or invalid claims, escalating problems to the generative supervisor. It cannot write objections.

Other uses include semantic ranking, claim validation, and intent routing.

A live call proved authentication and typed decoding for one case. Supervision quality, latency, false escalations, and costs remain unverified. Evaluate Jev offline and in shadow mode before live interventions.
