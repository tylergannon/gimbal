# Owned Go harness and parallel delivery plan

Read /Users/tyler/src/gimbal-pi-research/ephemeral/projects/pi-go-research/BRIEF.md first.

Draft a concrete, source-backed plan for owning an embeddable Go coding harness inside Gimbal. Inspect the upstream TypeScript modules, plausible Go ports/libraries, and Gimbal's actual adapter/session/event contracts yourself. The other two reports run concurrently; do not wait for them or pretend their verdicts exist. Label choices that must be reconciled with their findings.

Compare an owned copy of the strongest Go candidate, selective reuse plus semantic port, and a faithful fresh semantic port. Explain which choice changes the actual work and ongoing maintenance, including upstream synchronization and license notices. Do not assume a fork is cheap or an existing library is correct. Preserve useful Pi behavior and identify optional scope explicitly.

Tyler wants internal packages whose ownership allows roughly 10–20 coding agents to work concurrently, and is willing to use frontier planning plus abundant implementation capacity. Propose cohesive packages and disjoint file ownership grounded in real upstream responsibilities. Do not manufacture twenty abstractions to fill slots. Describe the foundation contracts that must settle first, the packages that can then run independently, necessary dependency ordering, and integration/qualification work that remains serial. Provide a proposed dependency diagram and an assignment table: package responsibility, upstream source/test references, consumed and produced contracts, ownership boundaries, observable acceptance behavior, dependencies, and uncertainty. Explain whether 10, 15, or 20 genuinely independent assignments are justified.

Keep provider transport and stream decoding, agent execution, coding tools, session/history, context/compaction, configuration/resources, and Gimbal integration ownership explicit where the source supports those seams. Ensure cancellation, streaming events, tool results, IDs, and errors have compatible contracts across workers. Respect the existing no-reflection rule and use ordinary Go rather than introducing another orchestration framework. Do not export new Gimbal API names or write implementation code in this research stage.

Propose tests and real coding workloads that can distinguish semantic equivalence from a smoke prompt: read/edit/run, multistep tool use, malformed/partial provider streams, retries, steering, cancellation cleanup, concurrent isolated sessions, forks, restart/resume, compaction, schema re-ask, and truthful usage. Cover differential testing against pinned upstream and portability. Discuss measurements for runtime/startup/memory benefits without inventing numbers. Distinguish what might be built today from what could actually be qualified today; show the critical path and blockers, not an unsupported calendar promise. Deliver a tractable proposal ready for the frontier model to reconcile and turn into implementation assignments after research completes.
