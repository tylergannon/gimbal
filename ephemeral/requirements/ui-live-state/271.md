# #271: Interrupted turn or command is drawn as failed; folded running scope shows 0m 00s

State at preparation: OPEN

`web/src/lib/run/layout.ts` and `Node.svelte`: a turn or command with `interrupted: true` gets the red failed pip. Per `docs/design/milestone-1-storybook.md` claim 9 an interrupted turn or command is ended, not failed and not waiting. Separately, a folded running scope's elapsed time shows `0m 00s` in `Sheet.svelte`'s folded summary.

Found by the build-frontend validator on the new run interface (PR #267, merged as a533c17). Small gap under the 90 to 95 percent rule; listed, not blocking.

No issue comments at preparation.
