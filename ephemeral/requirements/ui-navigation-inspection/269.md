# #269: Workspace topbar: elapsed time does not tick, search and current-activity link are unwired

State: OPEN

On `/runs/[runID]` (`web/src/routes/runs/[runID]/+page.svelte`, `web/src/lib/run/Topbar.svelte`): the elapsed time renders once and never updates while the run is live; the search box (`Find a scope or turn`) does nothing; the `now coding · task 3` link does not select or scroll to that node. The design (`docs/design/README.md`, `specimens/Main.html`) has all three working.

Found by the build-frontend validator on the new run interface (PR #267, merged as a533c17). Small gap under the 90 to 95 percent rule; listed, not blocking.
