# #270: Detail pane: command detail shows stdout or stderr, not both; Watchers and Definition disclosures are empty; submitting a form resets the selection

State: OPEN

In `web/src/lib/run/DetailPane.svelte` and the workspace page: a selected command shows one stream when both stdout and stderr exist (the record has both, with tails and files past 64 KiB); the Watchers disclosure renders nothing and Definition has no expanded content; submitting a steer, interview answer, or loop message resets the pane to the run overview instead of keeping the selected node. Stories for the command, scope, and loop variants are also missing (`DetailPane.stories.svelte`).

Found by the build-frontend validator on the new run interface (PR #267, merged as a533c17). Small gap under the 90 to 95 percent rule; listed, not blocking.
