# #272: Runs page: no loading state, and a long failure summary stretches the table past the viewport

State: OPEN

`web/src/routes/+page.svelte` with `web/src/lib/run/RunsList.svelte`: the loading skeleton from `SmallStates.svelte` is never shown while the Go load runs; a run whose ending summary is long (a failed run with a long error) widens the table to ~10,000px so the time and open columns need horizontal scrolling. Clamp the summary column and show the skeleton during load.

Found by the build-frontend validator on the new run interface (PR #267, merged as a533c17). Small gap under the 90 to 95 percent rule; listed, not blocking.
