# Bundle 2: workspace navigation and inspection

Implement the issues in /Users/tyler/.codex/worktrees/803e/gimble/ephemeral/requirements/ui-navigation-inspection/269.md and /Users/tyler/.codex/worktrees/803e/gimble/ephemeral/requirements/ui-navigation-inspection/270.md together. Read repository instructions and the existing design/source. Issue 272 is outside this implementation.

The live topbar elapsed time advances without incoming events and stops for ended runs. Search finds scopes and turns; choosing a result selects it in the map and detail pane and reveals it, including inside folded scopes. The current-activity control selects and reveals the active node. Keep coordinated selection when fresh observation data arrives and after a steer, interview answer, or loop message is submitted; reset appropriately when moving to a different run or when the selected item no longer exists.

Command detail exposes both stdout and stderr and the available complete-output references for truncated streams. Watchers and Definition disclosures show the available recorded information, with honest empty states where no information exists. Add the missing command, scope, and loop stories. Preserve the live connection, interruption, and folded timer behavior already fixed in bundle 1.

Demonstrate these behaviors in Chromium using the actual workspace page and a freshly built app on an isolated port. Exercise all three submission types through the real form/refresh path. Tests with deterministic data are useful, but an independent validator must inspect what their observations establish and personally exercise the changed built application. Existing saved runs in .gimble may help inspect real data. Do not accept aggregate green checks as evidence for unrelated claims. Report any behavior that remains unproved honestly.

Repository delivery checks: just build, just vet, just test, just fmt-check, and just e2e against the fresh build. A cancelled run fixture can be copied from internal/observation/testdata/cancelled-run.jsonl into .gimble/runs/e2e-cancelled/run.jsonl if E2E requires one. Generated files are generator-owned. Do not change docs/, the workflow definition, role defaults, or this definition of done. No committed proof programs or run output. Keep changes scoped and simple; tests belong beside the relevant code. Do not commit, push, merge, install, or remove the worktree; the parent agent owns delivery. Other agents may inspect the repository but this workflow owns bundle 2 implementation.
