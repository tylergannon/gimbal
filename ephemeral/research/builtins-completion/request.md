Complete the missing builtin workflows from the prior delivery. The user explicitly clarified Semantic Index building means a builtin Gimble workflow, not refreshing the research index.

Sprint planning must orient to repository/recent planning/context, discover and retrieve relevant prior art from a configured semantic index, save shared intent, run three independent drafts and cross-critiques, show their differences, interview as needed beyond one question, and save synthesis decisions plus final plan. Use the project's planning conventions when present; do not hard-code Diffusion ledger/CLI machinery. Preserve the original goal, acceptance, constraints and fixed commands.

Semantic Index must build a reusable local routing tree over a read-only local source cache using scoped parallel agent reads and cited leaves, with an entrypoint, routes, valid citations, source freshness/coverage/debt, and a usable incremental update path. Demonstrate retrieval from the actual index; structural reachability metrics are not proof of useful retrieval. Expose a direct CLI entrypoint and make the result available to Sprint planning. Keep this ordinary Go with constant workflow sites and keys, no new root API primitives, no generic scheduler, no remote sync.

Use cheap native models for live proof. Test harmful path overlaps, invalid citations, missing inputs, stale/deleted sources, and failure preserving previous valid index. Preserve all artifacts locally. Avoid replacing the requested behavior with a handful of generic prompts or claiming omitted steps are complete.
