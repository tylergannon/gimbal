# Gimble feature inventory research

Produce a readable, comprehensive inventory of the features currently implemented in Gimble, to support daily agent-driven end-to-end validation and other product documentation. Inspect /Users/tyler/.codex/worktrees/c187/gimble at commit 3db61eb99397fa6e01898b0746529b5fefbd3e14, which matched origin/main and the installed binary at research start on 2026-09-19. The repository is the authority; legacy material is not the current product. This is research, not implementation or a claim that features passed live tests.

Use exactly five coherent research groups covering the whole current product: CLI and built-in workflows; browser runs/graphs/details/control; conversations and harness capabilities; Go workflow runtime/context/loops/services; persistence/observability/tooling/build interfaces. Inspect actual source, public Godoc, help, and tests. Reconcile README, docs/web-app.md, and design records with code: they can be stale. Include new scope-owned Service behavior, finite Iterate and adaptive PromiseLoop, Check, Interview, Codex conversation restart, and harness differences. Distinguish implemented, partial, and planned/absent behavior; do not inflate the inventory with legacy features or treat tests as live proof.

Organize the final document into readable feature-family tables. Give each testable feature a stable ID, plain-English description, supported surface (CLI, browser, library, or combinations), implementation status/limits, a concrete observable end-to-end check, and current source references. Group related details without burying distinct behaviors. Separate features that cannot be directly exercised through CLI/browser from those the two frontends expose; identify where a small real Go workflow is required to exercise library behavior. Note provider/auth prerequisites and features requiring human answers, concurrent activity, cancellation, restart, or isolated fixtures. End with explicit coverage gaps and a concise inventory summary. This inventory will feed a later JSON or YAML feature file; do not design a framework or implement it now.

Only write research notes, Markdown source excerpts, indexes, and the assigned output. All cached source excerpts must use .md or .txt extensions, never compilable or executable file extensions. Do not modify product code, generate code, commit, push, create PRs, or launch modifying built-in workflows. Other research is running in the same worktree; write only within your assigned research directory and output file. Each excerpt must identify its original source path and the inspected revision. Be thorough but keep the final document within its supplied token budget.
