# OpenCode harness adapter research

Produce a technical compendium that lets Gimbal's maintainer decide whether to drive OpenCode's CLI directly or manage an OpenCode server, analogous to the current Codex app-server integration. Research only; do not implement an adapter or change application code.

Use current official documentation, upstream source, generated API/schema definitions, and relevant upstream issues. Identify the exact OpenCode project, release/tag and source commit studied; distinguish released behavior from moving-branch behavior. Download crucial evidence locally and cite both local files and original URLs. Keep downloaded source and any probe output under the supplied .gimbal research directory, not tracked ephemeral notes. The final report is authored research, not a transcript or run log.

Read the actual local Gimbal contract and adapters: /Users/tyler/.codex/worktrees/c671/gimbal/gimbal.go (or locate HarnessAdapter if moved), /Users/tyler/.codex/worktrees/c671/gimbal/session.go, /Users/tyler/.codex/worktrees/c671/gimbal/codex/, /Users/tyler/.codex/worktrees/c671/gimbal/claude/, /Users/tyler/.codex/worktrees/c671/gimbal/agy/, and /Users/tyler/.codex/worktrees/c671/gimbal/internal/binding/. Use go doc for the current public contract. Map OpenCode onto CreateSession, RunTurn, Steer, Fork, Close, typed JSON Schema output, TurnResult usage/cost, AgentEvent and NativeRef, continuation and cancellation semantics. Do not confuse Gimbal's normalized OpenCode-style event names with evidence that a native OpenCode adapter already exists.

Cover CLI noninteractive execution, machine-readable output and exact event envelopes, stdin behavior, continuation/fork, model/provider/reasoning selection, cwd, configuration/auth discovery, permission/question handling, error/exit behavior, cancellation and process ownership. Determine whether CLI run starts an embedded server, attaches to a server, or loses useful events/control.

Cover server launch/readiness/auth/bind address, process lifecycle and ownership, OpenAPI/SDK support (especially Go), all integration-relevant HTTP methods and routes with request/response shapes, session create/read/list/delete/fork, synchronous/asynchronous prompting, abort, status, permissions/questions, model catalogs/config, SSE subscription and scope. Explain how turn boundaries, IDs, ordering, reconnect/replay, completion/error, concurrent sessions and directory isolation work. Identify what is and is not supported for true mid-turn steering; queued follow-up is not landed steering.

Include a reference catalog of relevant event types and payload fields: session/message/part lifecycle; text/reasoning deltas vs snapshots; tool pending/running/completed/error; permission/question flows; usage/cache/cost/finish reason; retries, compaction, errors and terminal status. Preserve representative small JSON envelopes, identifiers, correlation rules, and source references. Explain which events CLI omits or transforms compared with SSE, and how to avoid duplicate output and stale completion races.

Compare CLI, managed local server, and attaching to an existing server against the contract. Recommend the smallest viable integration with explicit tradeoffs, material unknowns, and a short implementation and live-validation strategy. Distinguish documented/source-confirmed findings from observations; do not claim a live model turn was tested unless actually observed. Any optional live model probes must use the cheapest appropriate model. No need to install software or spend on OpenCode inference for this research. Do not commit or push; the caller will review the completed report and do that.
