# OpenCode newer API and generated Go client research

Run a second, focused research document for Gimbal's maintainer. The decision is how to build an adapter around OpenCode's NEWER `/api` server API and which current OpenAPI-to-Go generator actually works best. Prefer the newer API as the foundation; do not make combining legacy and new APIs the default architecture. Research only: no application changes or adapter implementation. Small executable generation and behavioral probes are explicitly required and authorized; keep them in the supplied .gimbal research cache, never ephemeral. Do not commit or push; caller handles delivery.

Read the previous report at /Users/tyler/.codex/worktrees/c671/gimbal/ephemeral/research/opencode/harness-adapter.md for context, not unquestioned authority. The new cache is /Users/tyler/.codex/worktrees/c671/gimbal/.gimbal/research/opencode-next. Its input/README.md and original opencode-generate.json document the successful local `opencode generate` run against 1.18.31. It contains 162 paths including 51 `/api` paths; opencode-new-api.json filters paths only and retains all components. Local Gimbal contract is in input/gimbal-api.txt and actual root/session/adapter code. Three independently running Luna generator experiments will produce trials/ogen/RESULT.md, trials/oapi/RESULT.md and trials/alternatives/RESULT.md. Read trials/INDEX.md. Those directories are owned by other agents; do not write there. Ingest their completed evidence through topic indexes before final synthesis; caller will announce completion. Do not invent outcomes for unfinished trials or rerun the same experiments without a concrete reason.

Required coverage:

1. New API functionality against the actual Gimbal HarnessAdapter contract: session creation/continuation/model/agent/location configuration; prompt admission and completion; same-active-RunTurn steering versus queued follow-up; interruption/cancellation; native fork/branching; schema-constrained output; event projection, usage, controls, cleanup. Investigate the previously reported fork and schema gaps in depth using exact released source and moving upstream source separately. Look for supported alternate NEW API operations (history/context/message APIs, tool configuration, etc.) that genuinely satisfy the contract, but do not equate history copying with native fork or prompt instructions with schema enforcement. Mark supported, source-only/unproven, missing, and possible upstream work distinctly. State whether a complete new-API-only adapter is feasible now and what decisions remain.

2. OpenAPI source completeness and reproducibility: actually inspect the CLI-generated original; compare server `/doc`, published/pinned repository spec and how generation composes older/newer APIs. Determine whether all relevant requests/responses/events are represented and accurate, especially SSE data, durable/live envelopes, WebSockets, unions, const tags, nullable fields, operation IDs, endpoint security and error statuses. Identify required information outside OpenAPI and whether `/api` subset generation can preserve dependency closure and remove legacy models. State exact spec/tool versions and hashes, tag resolution, stable release vs main. The parent already performed opencode generate; do not duplicate needlessly.

3. Current Go SDK generator comparison with actual experimental results. Go-written generators and generators emitting Go are both worth listing, clearly differentiated. At minimum ingest ogen, oapi-codegen/experimental, and OpenAPI Generator Go experiments. Survey other credible options, including hosted ones only if useful; no signup/credentials/cloud uploads. Grade (a) generation success AND compilation AND semantic correctness separately, (b) generated Go file count/LOC/bytes with consistent exclusions and separate tests/docs/dependencies, (c) configuration/pruning/templates/overlays/type mappings/client-only generation, and (d) actual handling of OpenCode's many discriminated unions. Use real OpenCode-shaped encode/decode probes (known/missing/unknown discriminator, nested variants, nullable/optional, preservation of payload fields) instead of accepting marketing claims. Explain each failure and workaround, original versus transformed input, any semantic loss, and whether the remedy is maintainable. Include exact repeatable commands/configuration and a compact graded comparison table. Untried candidates must be labeled untested rather than graded as successes. Latest is not automatically best; recommend based on generated results and maintenance burden.

4. Integration facts codegen may not solve: SSE framing/reconnect/cursor/deduplication, live controls vs durable records, HTTP context cancellation vs interrupting server execution, Basic auth/transport customization, errors/retries/idempotency (unsafe prompt retries), terminal output/usage correlation, process ownership and version compatibility. Inspect actual generated client boundaries; decide the smallest handwritten portion needed beyond generated code. No speculative SDK framework or adapter implementation.

5. Additional material unknowns and recommended next experiment. Focus on new-API stability/experimental status and upgrade/regeneration costs, API coverage drift, and cheap live verification. No OpenCode model inference is needed for this research. No-model local servers/probes may be used with bounded cleanup. Do not turn the report into an implementation checklist or prescribe legacy fallback without explaining the explicit tradeoff.

Use downloaded primary sources with original URLs, current official docs and exact released source. Preserve crucial data/probe logs under .gimbal; final report is authored notes, not run output. Research lanes own their topic directories and topic indexes ONLY; only the author writes the requested final report and the curator owns the combined INDEX.md. Aim for a focused compendium that answers these remaining decisions, not a rewrite of the earlier report. The independent editor must check that generation really occurred and distinguish generation from compilation and union correctness. Report must be self-contained enough to make the choice; local citations should resolve from its final path.

User follow-up: they previously built substantial custom OpenCode SDK generators using libopenapi last year. Evaluate libopenapi as a parser/model foundation for narrowly custom Go generation if current tools still fail; distinguish it from an off-the-shelf SDK generator. Prior code location is pending from user. Compare the actual remaining effort and union coverage without assuming a custom generator is necessary.

User clarification: prior libopenapi code is gone and belonged to their employer. Do not search for or reuse it. Their remembered strategies can inform a future HITL-heavy rebuild. Their main lesson was to stop forcing unsuitable tools; code generation itself was manageable. Keep workaround exploration bounded and recommend custom generation if it is cleaner, rather than sunk-cost patching.

User additional requirement: Their prior oapi-codegen generated types were useful; client/server endpoints were unsuitable, so custom generation supplied the SDK methods. Strong preference to reuse generated structs and unions from oapi-codegen or ogen, generating only HTTP/SSE/client glue ourselves if needed. Two independent models-only trials are now running in existing trials/oapi and trials/ogen; final report must incorporate this split rather than treating whole-SDK failure as proof types cannot be reused. Assess complete newer API type coverage, compilation, real event unions, and size. Do not implement a custom generator in this research.

Recovery after first follow-up run: run01M2XKS9JCGH2NE4R4KHEX93MV.research-document failed at index curation due agy result arriving with an unsettled tool step. All collection and generator trials are complete. Reuse existing topic001-010 source files and trial evidence; do not reinstall or rerun generator experiments. Researchers should audit their assigned evidence and repair indexes, then return promptly. Final authoritative trial results include COMPONENTS-ONLY oapi-codegen success (477364bytes14448lines, new event probes pass), full51path SDK failure, models-only with paths failure, partial ogen output. Read trials/alternatives/EVIDENCE-REVIEW.md and trials/ogen/REVIEW-NOTES.md, TOOL-COVERAGE.md, TYPES-ONLY.md. Author/editor must independently verify consequential claims from primary files, not trust index prose. The combined index currently includes suspect /api/version and /api/instance/dispose paths absent from exact spec; verify all route claims. No additional experimental work needed except resolving a concrete evidence gap.
