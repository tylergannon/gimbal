# Gimbal Defaults and Workflow Roles Source Excerpts

Origin: /Users/tyler/.codex/worktrees/d798/gimbal
Date: 2026-09-23

---
## 1. cmd/gimbal/defaults.json (full content)

```json
{
  "product-operation": "claude-opus-5-5:high",
  "product-visual-review": "gemini-3.8-flash-medium",
  "product-triage": "gpt-6-astra:high",
  "code-review": "gpt-6-luna",
  "sprint-planning": "gpt-6-astra:high",
  "coding": "gpt-6-sol:high",
  "architectural-critique": "gpt-6-astra:medium",
  "qa-orchestration": "gpt-6-sol:high",
  "research-planning": "gemini-3.8-flash-medium",
  "research-indexing": "gemini-3.8-flash-medium",
  "index-curation": "gemini-3.8-flash-medium",
  "document-authoring": "gemini-3.1-pro-high",
  "editorial-review": "gemini-3.1-pro-high",
  "document-supervision": "gemini-3.8-flash-medium",
  "pyramid-planning": "gpt-6-luna"
}
```

---
## 2. Review Workflow Roles (internal/workflows/review/review.go)

```go
// ReviewParams describes what to review.
type ReviewParams struct {
	// Goal says what the review should assess.
	Goal string
}

// Review asks one reviewer to inspect the repository read-only and record its findings.
func Review(ctx context.Context, env gimbal.Env, params ReviewParams) error {
	gimbal.Set(ctx, "goal", params.Goal)
	reviewer := gimbal.NewSession(ctx, gimbal.RoleCodeReview, env.WorkDir)
	result, err := reviewer.Generate[Result](ctx, reviewPrompt)
...
```
Role declared:
- `code-review` (`gimbal.RoleCodeReview`)

---
## 3. Implementation Workflow Roles (internal/workflows/implementation/implementation.go)

```go
const roleCoding gimbal.WorkflowRole = "coding"

// Params identify the ordered outcomes and the per-outcome task bound.
type Params struct {
	// OutcomesFile is a local JSON array of outcome strings, in execution order.
	OutcomesFile string
	// MaxTasksPerOutcome bounds planner assignments for each outcome.
	MaxTasksPerOutcome int
}
```
From `internal/workflows/implementation/workflow_gen.go:120-141`:
- `sprint-planning` (`gimbal.WorkflowRole("sprint-planning")`)
- `architectural-critique` (`gimbal.WorkflowRole("architectural-critique")`)
- `coding` (`gimbal.WorkflowRole("coding")`)
- `qa-orchestration` (`gimbal.WorkflowRole("qa-orchestration")`)

---
## 4. Validate Product Workflow Roles (internal/workflows/validateproduct/validateproduct.go)

```go
// Roles: product-operation defaults to Claude Opus 5.5, product-visual-review to
// Gemini Flash, and product-triage to GPT-6 Astra. Each has its model override flag.
```
From `internal/workflows/validateproduct/workflow_gen.go:143-157`:
- `product-operation` (`gimbal.WorkflowRole("product-operation")`)
- `product-visual-review` (`gimbal.WorkflowRole("product-visual-review")`)
- `product-triage` (`gimbal.WorkflowRole("product-triage")`)

---
## 5. Research Document Workflow Roles (internal/workflows/researchdocument/researchdocument.go)

```go
const (
	roleResearchPlanning    gimbal.WorkflowRole = "research-planning"
	roleResearchIndexing    gimbal.WorkflowRole = "research-indexing"
	roleIndexCuration       gimbal.WorkflowRole = "index-curation"
	roleDocumentAuthoring   gimbal.WorkflowRole = "document-authoring"
	roleEditorialReview     gimbal.WorkflowRole = "editorial-review"
	roleDocumentSupervision gimbal.WorkflowRole = "document-supervision"
)
```
From `internal/workflows/researchdocument/researchdocument.go:52-57`:
- `research-planning`
- `research-indexing`
- `index-curation`
- `document-authoring`
- `editorial-review`
- `document-supervision`

---
## 6. Pyramid Summary Workflow Roles (internal/workflows/pyramidsummary/pyramidsummary.go)

```go
const (
	roleDocumentAuthoring   gimbal.WorkflowRole = "document-authoring"
	roleEditorialReview     gimbal.WorkflowRole = "editorial-review"
	roleDocumentSupervision gimbal.WorkflowRole = "document-supervision"
	rolePyramidPlanning     gimbal.WorkflowRole = "pyramid-planning"
)
```
From `internal/workflows/pyramidsummary/pyramidsummary.go:50-53`:
- `document-authoring`
- `editorial-review`
- `document-supervision`
- `pyramid-planning`
