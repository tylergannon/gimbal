# Final adversarial review: Sprint Plan and Semantic Index builtins

**Target:** The current implementation in this worktree against `ephemeral/research/builtins-completion/request.md`, repository instructions, and the two copied source skills. I considered the complete requested behavior despite the caller's note about benchmark/rebalance modes; the request does not require porting those Diffusion modes.

**Evidence inspected:** Full relevant diff and new planning/index/CLI files; surrounding Gimble API and CLI; focused planning, semanticindex, cmd and agy tests (passing after the last fix); `go vet ./...` and `git diff --check`; the 12-source native index build and durable run record; initial source checksum snapshot; independent three-query retrieval request, transcript and answers; native changed/deleted-source update plus stale/clean audit artifacts; installed bounded route pages and a fresh paged-route query walk; native agy tool-ERROR raw stream and focused Gimble `run-prompt` proof; three native Plan attempts, with the third finishing successfully. No paid model calls were initiated by this reviewer.

**Index verdict:** No material index or CLI finding remains. The native build indexed 12 of 12 text sources with zero debt, emitted 12 reachable cited leaves, wrote a planning project pointer, and left the source corpus byte-identical to the prebuild snapshot. A blind worker started from the README and followed routes/leaves/source lines for three questions, returning the fixture's independently fixed 42-minute/no-failed-refresh, 06:20–06:40 UTC, and zero-based 7-to-28-second retry facts. A native stale audit failed without index writes; update reread one changed source, removed one deleted leaf, retained ten unchanged leaf bytes, and installed eight bounded route pages; a clean audit passed without model calls or writes. A fresh worker followed README → root routes → page-0003 → retry leaf → source line 3 and answered the policy correctly. These are behavioral observations beyond structural reachability.

**Planning verdict:** No material planning finding remains. The final native Plan run (`ephemeral/attest/planning-index/plan-final-output.txt`) automatically loaded the project-configured paged index, saved a fact-only orientation and shared intent with the goal, acceptance and fixed check, then completed three independent drafts and three cross-critiques. Its synthesis displayed consensus, differences and the unresolved filename tradeoff before asking the person. The exact answer, `retry.py` at the project root with `test_retry.py` beside it, was saved in `answers.md` and used in a second synthesis turn. That final turn produced nonempty, actual `Overview` and `Decisions` in `merge-notes.md`; `plan.md` preserves the zero-based shared retry policy, source/route citations, original acceptance and the exact `python3 -m unittest discover` command. The run ended `ok` without implementing or running the future test. Earlier provider 503 and unsettled-tool failures did not yield a final plan; the latter was diagnosed from raw native `state:"ERROR"` plus `tool_info.error` (`agy-error-probe/raw.jsonl:4-8`) and fixed narrowly. A separate native `run-prompt` probe demonstrates failed-tool closure followed by a valid read and successful run, while truly unfinished ACTIVE steps still reject success.

**Outcome:** no findings.
