decision: Review only, at merged PR 140 SHA 2a52971; no production fixes or commits authorized.
friction: ScopeText is the only public scoped read operation; task feedback includes direct task values only. Probe nested validation and document explicit result promotion.
decision: Exercise a fresh dependency-aware merge-queue CLI fixture rather than changing another live repository. Codex gpt-5.6-luna plans and codes; Claude Haiku validates. Fixed external black-box acceptance covers ordering, dependency blocking, invalid inputs, cycles, and generated DAGs.
friction: Fresh detached checkout lacks generated web manifest; initial race suite fails only web bootstrap checks. Run just build before the complete suite (already tracked as issue 112). Built race suite and vet passed.
correction: Ending a task scope marks sessions closed but does not release an unused native Codex fork. Reproduced with actual Codex adapter and a mock app-server process; child remained alive after Run returned and was explicitly terminated by the probe. Existing issue 108 remains relevant.
friction: The real Go cancellation event stream reduces to failed in the TypeScript observer, while the server checkpoint preserves cancelled. Restore the same terminal-state rule on both sides.
friction: An Output validation failure is returned after TurnEnded already recorded success; a Go return error inside Loop's range body likewise records empty task/loop scope errors. Observation must distinguish lifecycle termination from validated success.
friction: Eight malformed backlog responses caused nine planner calls and zero yielded tasks; a task budget in the range body cannot bound Loop's internal repair traffic.
friction: The actual Luna planner hit YAML scanner failure while adding the parallelism assignment (unquoted colon in description). Loop recovered with a 25-second extra repair turn. This makes the dual YAML-edit/JSON-answer protocol's complexity concrete, independently of the fake perpetual-invalid probe.
decision: The base merge-queue implementation passed all 25 external cases and Haiku's assessment. New parallelism requirements were disclosed only after this, producing 0/6 passes and a new adaptive task. This is an intentional changing-requirement experiment, not a hidden defect in the initial task.
decision: Live Loop completed in about 15m26s: two useful assignments, four planner turns including YAML repair, two Haiku PASS assessments, 25/25 base cases and 6/6 added parallelism cases. Final planner STOP was followed by independent command gates; Run returned nil and the final durable Complete reported no recording error. This supports the primitive while leaving long-duration/runtime-control claims unproved.
