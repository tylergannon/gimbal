correction: Issue 162 distributes one existing gimble binary. `gimble lint` and direct `go vet -vettool=/absolute/path/to/gimble` route into the analyzer before ordinary CLI parsing; there is no cmd/gimblelint binary or module tool.
decision: Vet protocol config routing validates the final .cfg as a unitchecker config and gives explicit run-prompt and server options precedence, because a suffix-only check stole ordinary CLI requests.
friction: Early duplicate fixtures used non-generic Set stubs and missed real SSA generic callee behavior -> fixtures now use generic Set and SetJSON, resolve SSA origins, and cover captured outer contexts in synthetic range-over-func bodies.
decision: Dynamic worker analysis follows recognized callbacks into every directly reachable source-visible package helper, including helpers without a context parameter. It identifies worker callables by their context.Context parameter and does not follow public Gimble API calls into runtime internals, avoiding findings for ordinary callbacks such as validation and cancellation functions.
decision: The builtin sprint's formatted repository-check key remains unchanged. Its GIMBLE102 failure is required negative acceptance, so just vet is expected to remain nonzero and proof must show that result.
decision: The observer's final built-binary probes passed 17 checks. Compare linter size using identical frontend assets: clean-worktree Go builds grew by 5,540,176 bytes; the final just-build binary also embeds the frontend and is 33,164,322 bytes.
