correction: Issue 162 distributes one existing gimbal binary. `gimbal lint` and direct `go vet -vettool=/absolute/path/to/gimbal` route into the analyzer before ordinary CLI parsing; there is no cmd/gimballint binary or module tool.
decision: Vet protocol config routing validates the final .cfg as a unitchecker config and gives explicit run-prompt and server options precedence, because a suffix-only check stole ordinary CLI requests.
friction: Early duplicate fixtures used non-generic Set stubs and missed real SSA generic callee behavior -> fixtures now use generic Set and SetJSON, resolve SSA origins, and cover captured outer contexts in synthetic range-over-func bodies.
decision: Dynamic worker analysis follows recognized callbacks into every directly reachable source-visible package helper, including helpers without a context parameter. It identifies worker callables by their context.Context parameter and does not follow public Gimbal API calls into runtime internals, avoiding findings for ordinary callbacks such as validation and cancellation functions.
decision: The builtin sprint's formatted repository-check key remains unchanged. Its GIMBAL102 failure is required negative acceptance, so just vet is expected to remain nonzero and proof must show that result.
decision: The observer's final built-binary probes passed 17 checks. Compare linter size using identical frontend assets: clean-worktree Go builds grew by 5,540,176 bytes; the final just-build binary also embeds the frontend and is 33,164,322 bytes.
decision: `internal/gimballint/rules.md` is the canonical rule reference. It is embedded in `Analyzer.Doc`, so `gimbal lint -help` carries it in the binary; `docs/lint-rules.md` is a relative symlink and README only links to the canonical text.
proof: Built `bin/gimbal` printed GIMBAL101 through GIMBAL107 and normal analyzer flags from `lint -help`, including after the binary was copied to an empty temporary directory. The docs symlink resolves to the canonical file with identical SHA-256 bytes; focused tests, stock vet, root lint, and diff check passed.
