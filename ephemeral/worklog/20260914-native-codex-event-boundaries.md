decision: Keep a raw-completed Codex response open until its delayed tool finishes, the next non-tool item begins, or the turn completes; existing native records show tool items can follow rawResponse/completed.
friction: `pnpm install` inside the retained issue-130 proof joined the repository workspace and did not link its local Playwright dependency -> use `pnpm install --ignore-workspace --frozen-lockfile` for this nested standalone proof package.
friction: codex-cli 0.153.4 accepts but ignores `experimentalRawEvents` on `thread/fork` and `thread/resume`; its generated protocol exposes the field only on `thread/start`, and a real fork produced no raw response events -> do not infer boundaries or reconstruct history; an app-server capability is needed for exact fork coverage.
friction: a second `Generate` on the existing shared-daemon connection does not call `thread/resume`; the adapter reserves that RPC for a redial after connection loss -> describe this live evidence as a second turn and leave native resume-after-redial unproven.
