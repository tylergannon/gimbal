decision: Keep a raw-completed Codex response open until its delayed tool finishes, the next non-tool item begins, or the turn completes; existing native records show tool items can follow rawResponse/completed.
friction: `pnpm install` inside the retained issue-130 proof joined the repository workspace and did not link its local Playwright dependency -> use `pnpm install --ignore-workspace --frozen-lockfile` for this nested standalone proof package.
friction: codex-cli 0.153.4 accepts but ignores `experimentalRawEvents` on `thread/fork` and `thread/resume`; its generated protocol exposes the field only on `thread/start`, and a real fork produced no raw response events -> do not infer boundaries or reconstruct history; an app-server capability is needed for exact fork coverage.
friction: a second `Generate` on the existing shared-daemon connection does not call `thread/resume`; source shows a socket-only redial retains the loaded thread's sticky raw flag, while resume after daemon restart or thread unload cannot re-enable it -> describe the live evidence as a second turn, leave socket-redial browser behavior unproven, and state the unsupported restart/unload case precisely.
correction: leaving a streamed response open until any delayed high-level tool completed handled only one tool and could attach a tool-first next response to the old step -> consume native `rawResponseItem/completed` call/output pairs and end only when the response's raw calls settle.
evidence: one live raw `functions.exec` call expanded to a file change and command execution that settled separately; both rendered in response `resp_05e477b55b7c99d0016aa85546e1f087d1a0e0400521260add`, while the following final answer rendered under `resp_05e477b55b7c99d0016aa8554e347c87d19d7ad1c6adda8370`.
