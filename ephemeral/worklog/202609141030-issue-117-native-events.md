decision: Preserve the normalized session event vocabulary; native harness errors project through session.retry.scheduled or session.step.failed, and native approval requests or denials through permission.asked and permission.replied.
decision: A native willRetry notification keeps the Codex turn alive because the app-server owns the retry; no retry timing or attempt is invented when the callback does not state one.
decision: Completed permission decisions remain projection history so the browser can show what was requested and how the fixed noninteractive policy answered.
friction: Real child notifications arrive after the parent tool has completed and a later parent step has started. The first live run rejected these events; persistent tool-to-message ownership now keeps the late transcript attached to the completed tool.
friction: Codex NativeRef accepts only the shared provider/session/turn/message/response/item ancestry keys. A first terminal-error run exposed an invented notification key, which was removed while retaining the native error fields in normalized event data.
fact: Codex approvalPolicy `never` makes request_user_input unavailable in Default mode. Claude bypassPermissions must run without WithCanUseTool because the SDK hook adds --permission-prompt-tool stdio and exposes AskUserQuestion; only passive native permission_denied messages are projected. Approval projection is regression-tested but cannot be claimed as live-observed under the required policy.
fact: A live Claude child using Bash emitted a user tool_result envelope with parent_tool_use_id. All envelopes owned by a known parent tool must remain nested, including user and system/result frames, while an unrelated ancestry ID must continue through the parent projector.
fact: The production browser must prefer late session.tool.progress transcript metadata over tool completion content because completion can contain only the prefix observed at that time.
friction: just vet's Go vet/build stages pass, but repository-wide gimbal lint reports five unrelated existing diagnostics in sprint workflow, old ephemeral workflows, and issue159 tests. They were recorded rather than changed outside issue scope.
friction: Opus found that both adapters copied the accumulated child transcript into every progress event, making the persisted delta stream quadratic. Progress now emits one append entry, while both reducers retain the complete transcript across completion and late events; 1,000-event regressions bound emitted payload size.
