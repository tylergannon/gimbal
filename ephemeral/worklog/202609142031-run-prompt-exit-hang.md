discovery: Current HEAD exits normally for real Flash, Fable, and GPT no-tool turns, and for a real Fable tool turn; the broad claim is not reproducible on every invocation.
design_bug: The agy adapter persists a valid terminal SUCCESS result but keeps RunTurn blocked until stdout reaches EOF, so a native process that remains alive after its terminal envelope leaves run-prompt alive and withholds stdout.
proof: A deterministic protocol-valid agy emitted init, a completed assistant step, and SUCCESS result within two seconds, then stayed alive; after fifteen seconds both gimbal run-prompt and agy were still alive, stdout was empty, and run.jsonl had turn_started but no turn_ended.
design_bug: The timeout is not a complete backstop because interruptProcess signals and kills only the direct agy process. A descendant that inherited stdout can survive reparented to PID 1 and keep scanStream blocked forever; a two-second timeout still had a live run-prompt and orphaned sleep after fifty seconds.
decision: Separate the common post-result delay from the unbounded bug. Every run-prompt waits for adapter teardown and root-scope session cleanup before printing and exiting; Claude cleanup is bounded by its SDK and Codex cleanup by the scope timeout, while only agy's EOF-before-Wait path was demonstrated unbounded.
proof: Three additional successful Fable run-prompt processes and one forced 12-second timeout left none of their exact Claude or Tractor descendant PIDs alive; the timeout run ended in 13 seconds with a complete lifecycle.
discovery: Seventeen multi-day ccd-cli processes belong to Claude Desktop remote server PID 1155, not Gimbal. Two PIDs 40137 and 93339 are finished run-cli sessions whose transcripts ended normally on 2026-09-10, whose shared worktree is clean and tracks a gone remote branch; they remain Claude-managed cleanup candidates, not evidence of a Gimbal leak.
decision: Treat agy's valid result envelope as the turn boundary, start Wait concurrently with stream projection, and isolate each native turn in its own Unix process group so terminal cleanup can close the stream, reap the command, and kill descendants that inherited its descriptors.
proof: The deterministic run-prompt fixture that previously hung exited 0 in one second, printed AGENT_FINISHED, recorded turn_ended through complete with no recording error, and left no matching gimbal, agy, or sleep process. The full Go race suite, go vet, Gimbal lint, web tests, just build, and a Windows cross-compile all passed.
