# Issue 227: extract the workflow graph

decision: Tyler 2026-09-15: delegate the build; the manager's job is to keep
over-engineering and made-up code out. Validation is manual: a subagent
witnesses the software working; nothing automates the proof.

decision: the run receives the graph through the ctx, `gimbal.WithGraph`,
beside `Project`; the graph's Name must be the run's name. Saved as
`graph.json` in the run directory before `run.jsonl` opens; not an
observation table.

decision: the generator is `gimbal graph -entry -name [-o]`, a subcommand
beside `lint`, and the directive is `go run github.com/tylergannon/gimbal/cmd
graph ...` (the binary builds on a bare checkout: web/ embeds build/.gitkeep).

decision: helper returns. A guard before a helper's first operation is
dropped (issue accepts commands reading unconditional); a late return in a
tail-position helper is the body's exit (runTask's `!passed`); error-nil
tests with no operations are dropped everywhere. Brief:
ephemeral/plans/issue-227-graph-build.md.

friction: `web.Runtime.Run` derived the run's ctx from the runtime's stored
ctx and used the caller's only for cancellation, so any value a caller put on
its ctx (the graph) was dropped silently -> the run's ctx now descends from
the caller's; the runtime adds its registry and runs table. Found by the
witness, not by any test.

decision: reassignment of a session, group, or loop identifier with `=` is a
diagnostic, never a rebinding. Sol's rounds one and two both found guesses in
rebinding across branches and callbacks; removing the path was smaller than
reconciling it. Source that wants a session chosen in a branch should be
rewritten, per the authoring rule.

decision: Go and a Tasks range patch their Group/Loop node only from the body
that declared it; from a branch it is a diagnostic. A conditional child is
not representable in the shape and is not guessed.

decision: nested Gimbal calls in call arguments, else-if headers, switch
cases, and for headers are diagnosed, not read.

correction: Tyler 2026-09-16: generated code wires itself. `gimbal.WithGraph`
is gone; the generated file's init calls `gimbal.RegisterGraph(Graph)` and
`Run` looks the graph up by the run's name. A workflow is wired to its shape
by being built, never by a line in main.

correction: Tyler 2026-09-16: no graph.json. "The graph is for visualizing
the thing via the binary"; someone reading a run with the binary gone has
the logs. A file we write becomes a public API to coding agents, who will
build on it instead of the registered graph. The run writes nothing; the
registry by name is the graph's only surface. Produce a file if and when
something needs it. Issue 215 is answered by RegisterGraph, not a file.
