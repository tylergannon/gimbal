decision: Tyler authorized starting #254 directly before the delivery workflow (#248), changing the earlier bootstrap order. Keep the implementation on a separate branch stacked on PR #246 so the infrastructure PR stays narrow.
decision: Loop(ctx, name) provides Iterations without a planner; Tasks(goal, planner) retains adaptive dispatch. Both reuse scope lifecycle cleanup, and data carried across iterations stays explicit in Go.
decision: The existing observed loop flag denotes planner dispatch and enables planner controls; plain iteration containers are ordinary observed scopes, with no planner inbox or backlog.
friction: The first lifecycle tests iterated empty bodies, so they did not establish session cleanup. Tests must open actual adapter sessions and assert Close before the next body or enclosing continuation.
correction: A shared Loop object conflated finite collection iteration with adaptive planner dispatch. Keep PromiseLoop as an object with Err; make Iterate a direct generic iterator over a slice, with one implicit scope per item and no Range helper until a workflow needs it.
