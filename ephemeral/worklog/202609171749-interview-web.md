decision: The run page groups gimbal.interviewDecision turns and interview rows into one node keyed by scope and session, because question lifecycle rows deliberately carry no turn id and the issue accepts the node key as an implementation choice.
friction: web package pnpm build resolved a Vite 6 CLI that rejected the SSR input, while the repository's documented pnpm exec vp build uses the pinned Vite Plus path and is the working build command.
