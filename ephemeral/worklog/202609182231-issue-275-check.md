# Issue 275: Check command evidence

correction: Check keys follow Set and SetJSON rules; they are explicit and unique per scope, so repeated checks use call-site keys such as tests.1 and tests.2 rather than runtime-generated context keys.
