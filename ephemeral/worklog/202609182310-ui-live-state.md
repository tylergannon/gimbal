decision: Implemented the selected #273 task only; the bundled #271 work and parent-owned delivery artifacts remain untouched.
friction: Direct web Go tests failed before the generated frontend build because skgo.manifest.json was absent; run the repository build before web boundary tests.
friction: The fixed ui-live-state E2E failed twice at the existing detail-pane selection assertion while the About scenario passed; the failure is outside the #273 connection lifecycle and is reproducible against the seeded workspace state.
