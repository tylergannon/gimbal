decision: Issue #284 research uses local repository contracts and implementation snapshots as the primary corpus; no external catalog was needed for the assigned baseline topics.
decision: The current graph already preserves root, named Scope, and Iterate nesting plus Source{File,Line}, but Service, RunCommand, and Check are all emitted as ordered workflow.Command nodes; treat that as the exact seam to inform the final recommendation.
decision: Keep runtime service semantics out of the static graph evidence: #276 readiness, failure, cleanup, output, and process-group behavior are runtime contract only.
friction: The requested topic directories were initially absent and the worktree already contained unrelated untracked prior research artifacts; harvested only the assigned topic files and did not alter unrelated research.
decision: For graph evidence, Graphviz clusters, Mermaid subgraphs, and BPMN subprocesses all support containment, but only BPMN explicitly separates sequence flow from non-sequence relationships; recommend a sibling service collection beside ordered Body rather than service nodes or edges.
decision: For UI evidence, React Flow/Svelte Flow parent-child containers and GitHub Actions job/service separation support a labelled Services strip inside each scope sheet; a dedicated lane would imply another execution stream.
friction: The downloaded product docs are app-rendered HTML with large embedded state, so normalized tidy copies and short evidence clips are needed for useful local citation without treating the HTML as a clean transcript.
