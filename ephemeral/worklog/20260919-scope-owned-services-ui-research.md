decision: Issue #284 research uses local repository contracts and implementation snapshots as the primary corpus; no external catalog was needed for the assigned baseline topics.
decision: The current graph already preserves root, named Scope, and Iterate nesting plus Source{File,Line}, but Service, RunCommand, and Check are all emitted as ordered workflow.Command nodes; treat that as the exact seam to inform the final recommendation.
decision: Keep runtime service semantics out of the static graph evidence: #276 readiness, failure, cleanup, output, and process-group behavior are runtime contract only.
friction: The requested topic directories were initially absent and the worktree already contained unrelated untracked prior research artifacts; harvested only the assigned topic files and did not alter unrelated research.
decision: For graph evidence, Graphviz clusters, Mermaid subgraphs, and BPMN subprocesses all support containment, but only BPMN explicitly separates sequence flow from non-sequence relationships; recommend a sibling service collection beside ordered Body rather than service nodes or edges.
decision: For UI evidence, React Flow/Svelte Flow parent-child containers and GitHub Actions job/service separation support a labelled Services strip inside each scope sheet; a dedicated lane would imply another execution stream.
friction: The downloaded product docs are app-rendered HTML with large embedded state, so normalized tidy copies and short evidence clips are needed for useful local citation without treating the HTML as a clean transcript.
correction: Generated workflow role flags are advanced overrides, never routine invocation inputs; the maintained `skills/gimbal-runs` source is correct, but the installed copy remains stale until the post-merge skill refresh.
decision: Static `workflow.Service` declarations live on every actual owner (`Graph`, `Scope`, `Iterate`, `PromiseLoop` task body, and `GroupChild`) and never implement `Operation`; `Repeat` and `Condition` keep collecting services into their enclosing owner.
decision: The web map renders a labelled, disconnected Services panel inside each owner and exposes constant name plus source location. Service runtime command rows remain valid history for graph matching but do not become command nodes or status pips.
decision: Folded owners summarize only the service count; this preserves visible ownership without introducing a runtime-status representation or crowding the existing step glyphs.
friction: The first browser-test run after importing a new Lucide icon triggered Vite dependency optimization and reloaded the test page mid-run; the unchanged rerun passed, so treat the first result as cache warm-up rather than a product failure.
proof: `just build`, `just test`, `just vet`, `just fmt-check`, the full Lefthook pre-commit suite, and the production Playwright E2E suite all passed; E2E artifacts stayed under `/private/tmp/gimbal-issue284-e2e`.
proof: Live Storybook inspection showed root, named-scope, and per-iteration service panels; selecting a declaration showed its owner and source without a status pip, folding retained the service count, and sequence connectors routed around rather than through service panels.
friction: The bounded `gimbal run review` pass ignored its concrete-finding definition of done and kept widening inspection after rerunning the relevant 41 web tests successfully. It was interrupted after 4m37s without a verdict, so it is recorded as workflow-control evidence rather than independent review proof.
