# Product validation workflow

decision: User requested implementation followed by exactly one adversarial review through gimble run-prompt, then feedback. No merge/install or iterative review loop is requested for this change.
decision: Keep project-specific preparation/startup/readiness, target addresses, and plain-English features in JSON/YAML input. Ordinary Go performs service ownership, per-feature recording, operation, independent validation, and reporting.
friction: Opus pilot established that Playwright CLI daemonizes, video-stop is mandatory, and cancellation cleanup needs a fresh context. GoTTY worked as the macOS terminal fallback. SIGKILL cannot guarantee cleanup or finalized video; preserve that limitation.
failure: The one-pass adversarial review found that publishing the report path in agent context caused validators to edit it and cite it as evidence during the live smoke. File existence alone does not bind evidence to the current feature. Preserve report ownership and constrain citations in the follow-up.
failure: Workflow-local success cannot certify harness-session cleanup: Scope records adapter Close failures in the run aggregate, which Run joins after the workflow returns. Completion reporting must account for that outer boundary. Both findings remain open for user review; no second review or fix loop was started.
decision: Fix the review without changing Gimble's scope cleanup contract. Feature reports no longer assert overall completion; callers use the final run result/CLI exit status, which already includes CloseError. Remove the report path from agent context, reject foreign/unlisted evidence, check original content after validator closure, and block/restore agent-edited reports.
failure: Fable 5.1 confirmed both original findings fixed, then reproduced two new regressions: rejecting a non-original citation loses the validator's fail reason, and rejecting an empty operator capture blocks judging even when empty output is expected. These remain open in round 02; do not treat the passing smoke as coverage for them.

correction: User clarified that video is for optional human review and asked for the 80/20 case. The second judging agent, video decoding/sampling, and evidence fingerprinting were assistant-added complexity. Remove them; use direct screenshots/CLI output and one exercising agent's result, retaining cleanup and basic attachment checks. Keep artifact errors separate from product findings and allow empty captures.
correction: Tyler clarified the intended agent run is a neutral user focus group, not exhaustive feature-by-feature verification. Give agents practical workloads (for example use Gimble to take a project issue through a PR), permit natural exploration, and collect task completion, elapsed time, ordered captioned screenshots, bugs/blockers, information quality, and up to three major UX frustrations. Do not recreate work suited to unit or E2E tests as detailed agent checklists.
decision: Preserve the 63-family inventory for code-review and automated-coverage work, classified by primary testing mechanism. For practical user tests, sequence isolated workload fan-out, Gemini Flash visual review of screenshots/captions, then a high-level agent that synthesizes findings and opens nonduplicate actionable GitHub issues. Flash checks visible evidence, not task correctness; video stays for human review. Initially report task success and UX separately within one session rather than building two workflows.
decision: The no-source rule applies to Gimble, the product being evaluated. Its public skills/docs are valid user aids; source in a separate target project is available when the workload is a coding task. Design recorded in the research worktree at ephemeral/research/daily-validation/user-testing.md; this latest direction is not yet implemented in validate-product.
