# Product validation workflow

decision: User requested implementation followed by exactly one adversarial review through gimble run-prompt, then feedback. No merge/install or iterative review loop is requested for this change.
decision: Keep project-specific preparation/startup/readiness, target addresses, and plain-English features in JSON/YAML input. Ordinary Go performs service ownership, per-feature recording, operation, independent validation, and reporting.
friction: Opus pilot established that Playwright CLI daemonizes, video-stop is mandatory, and cancellation cleanup needs a fresh context. GoTTY worked as the macOS terminal fallback. SIGKILL cannot guarantee cleanup or finalized video; preserve that limitation.
failure: The one-pass adversarial review found that publishing the report path in agent context caused validators to edit it and cite it as evidence during the live smoke. File existence alone does not bind evidence to the current feature. Preserve report ownership and constrain citations in the follow-up.
failure: Workflow-local success cannot certify harness-session cleanup: Scope records adapter Close failures in the run aggregate, which Run joins after the workflow returns. Completion reporting must account for that outer boundary. Both findings remain open for user review; no second review or fix loop was started.
decision: Fix the review without changing Gimble's scope cleanup contract. Feature reports no longer assert overall completion; callers use the final run result/CLI exit status, which already includes CloseError. Remove the report path from agent context, reject foreign/unlisted evidence, check original content after validator closure, and block/restore agent-edited reports.
failure: Fable 5.1 confirmed both original findings fixed, then reproduced two new regressions: rejecting a non-original citation loses the validator's fail reason, and rejecting an empty operator capture blocks judging even when empty output is expected. These remain open in round 02; do not treat the passing smoke as coverage for them.

correction: User clarified that video is for optional human review and asked for the 80/20 case. The second judging agent, video decoding/sampling, and evidence fingerprinting were assistant-added complexity. Remove them; use direct screenshots/CLI output and one exercising agent's result, retaining cleanup and basic attachment checks. Keep artifact errors separate from product findings and allow empty captures.
correction: Tyler clarified the intended agent run is a neutral user focus group, not exhaustive feature-by-feature verification. Give agents practical workloads (for example use Gimble to take a project issue through a PR), permit natural exploration, and collect task completion, elapsed time, ordered captioned screenshots, bugs/blockers, information quality, and up to three major UX frustrations. Do not recreate work suited to unit or E2E tests as detailed agent checklists.
decision: Preserve the 63-family inventory for code-review and automated-coverage work, classified by primary testing mechanism. For practical user tests, sequence isolated workload fan-out, Gemini Flash visual review of screenshots/captions, then a high-level agent that synthesizes findings and opens nonduplicate actionable GitHub issues. Flash checks visible evidence, not task correctness; video stays for human review. Initially report task success and UX separately within one session rather than building two workflows.
decision: The no-source rule applies to Gimble, the product being evaluated. Its public skills/docs are valid user aids; source in a separate target project is available when the workload is a coding task. Design recorded in the research worktree at ephemeral/research/daily-validation/user-testing.md; this latest direction is not yet implemented in validate-product.
correction: Supersedes the Gimble-specific phrasing above: for EVERY primary product under test (A), the user-testing agent must never inspect A's source; accidentally encountered source must be ignored in the assessment. When testing Gimble as A by assigning work in another project B, the tester may inspect B's source but should not normally need to. It should rely on Gimble to perform the work, including delegating B's implementation to Gimble's agents, and assess A through its public interface and observable results.
decision: Implement the clarified workload flow in the existing validate-product command, replacing the feature checklist. Three explicitly named tester slots accept one to three caller-supplied assignments, then one Flash screenshot review and one senior triage turn. No planner, dynamic worker dispatch, retry loop, nested judging loop, or custom report schema for agents; handoffs are local Markdown reports and a small workflow-written manifest. Existing graph extraction requires Group.Go calls in the declaring body, so fixed slots preserve a complete visible graph.
decision: Record tester execution errors without aborting independent workloads, and pass incomplete visual review/errors to triage honestly. An empty issue_repo provides a report-only rehearsal; a supplied repository authorizes the final agent to deduplicate and create issues there. Product defects are findings, distinct from agent execution/cleanup failures.

decision: Tyler requested repeated real workload validation before merge: create a skgo Scrabbler seed only, have tester agents use Gimble to implement features and monitor through its web UI/API, and stop once a playable 2/3/4-person game and useful validation reports exist at 90–95% completeness. Three sequential caller-supplied workloads cover first board, core rules, and actual browser play.
friction: The earlier sample used run-prompt --logs .gimble, but run-prompt requires a new store; that caused invisible runs and workaround churn. The real workload uses run implement in the same project store as its standalone web server. Keep tester instructions outside project B and pass only the coding feature definition to the child to avoid recursive testing assignments.
friction: A real scaffold task removed the active project .gimble store and the implementation process ended while the standalone UI/API retained a running snapshot. Tester instructions must use the process handle to resolve disagreement with web state, and scaffold assignments must preserve .git/.gimble when copying a generated app into an existing worktree. This is not grounds for repeated restarts based only on stale observation.
correction: Sharing .gimble with a standalone viewer is insufficient for live observation: the viewer caches an externally created run snapshot and disconnects its stream. Real workload assignments must open the running workflow's own web listener (run implement --port, not --no-web), then navigate the recorded browser there and query that listener's HTTP API. The initial suite URL may remain the history/start page. Preserve this distinction in example guidance.
friction: A long-running validation invocation keeps its original binary even after the branch is synchronized. The first Scrabbler trial produced a Flash report but failed on the old agy unsettled-step check; main already fixed that in #303. Rebuild before subsequent trials and preserve the first trial's error rather than treating streamed text as successful completion.
decision: The real trial produced actionable triage rather than another implementation checklist: stale foreign-run snapshots are filed as #307, observation-file failures belong to existing #304, and old agy unsettled-step failure was already repaired by #303. Keep these outside the validation workflow change; the run-owned listener works around #307 for subsequent workloads.
correction: Tyler explicitly requires at least Sonnet for actual building. Applying the cheapest-model live-smoke rule to substantial Scrabbler implementation wasted time and produced avoidable defects. Keep cheap models for observation/screenshot checks, but use Sonnet or stronger for implementation and its validation. This user instruction supersedes the earlier test-model assignment.

decision: Round 2 completed the entire validation workflow successfully even though its delegated game task remained incomplete. Flash opened 16 screenshots, corroborated the visible defects and flagged overclaiming captions; triage distinguished task failure from validation execution. The final repair used Sonnet for all implementation roles and completed in one task. Parent browser checks independently observed BE scoring 8, TRAIN scoring 12, exchange and 2/3/4-player turn rotation, and final adjusted scores/winner after consecutive passes.
