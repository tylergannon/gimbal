# Background model cost guidance

correction: Model cost guidance needs a second profile beyond a uniform
downgrade: spend frontier capability on a strong plan, then let cheaper Terra
or Sonnet workers grind on low-risk background tasks where wall-clock time and
rejected attempts are inexpensive.

decision: Recommend this only for reversible, independently validated work
with bounded feedback and attempts. Distinguish a completed attempt rejected by
validation from a provider, harness, or execution error; the latter does not
retry unless the workflow explicitly implements that behavior.
