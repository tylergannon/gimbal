decision: Legacy active-turn steering uses abort plus immediate continuation inside the same RunTurn; the newer API remains outside this adapter because research found no bridge into a running legacy turn.
friction: A real opencode/ling-3.0-flash-fin-free conversation returned an OpenCode 500 from native fork after text, schema, and continuation had succeeded -> inspect the owned server log and keep a native defect distinct from an adapter request defect.
decision: OpenCode-prefixed model selection leaves effort empty unless explicitly supplied, preserving OpenCode's proven default variant instead of inheriting Gimbal's raw-native high default.
friction: Two concurrent free-provider tool turns reached the two-minute caller deadline while the same run later proved steering, cancellation, and explicit stop -> retry concurrent routing with another configured free model and retain deterministic routing tests independently of provider queue behavior.
friction: A live file-tool probe named the macOS /var spelling while OpenCode canonicalized the project to /private/var, triggering an external_directory permission request and eventual cancellation -> live tool prompts should use a project-relative path when proving ordinary in-project writes.
