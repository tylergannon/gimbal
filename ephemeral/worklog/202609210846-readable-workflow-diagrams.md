correction: A workflow diagram is useful only if a reader can understand what the workflow will do; operation names and structural shapes alone are not enough. Generate nodes need resolved prompt content, and control-flow labels need plain-language meaning.
failure: Visual review accepted a mechanically simplified graph whose optional-command predicate rendered as corrupted Go syntax while the important validation-pass exit was omitted -> inspect rendered semantics, not merely successful SVG output or reduced size.
decision: Render prompt-bearing agent calls as readable actions with their actual prompt text, fold optional-command predicates into the command presentation, and retain loop exit conditions that explain completion and failure.
failure: Hiding Mermaid join nodes with transparent styles left arrows that visibly stopped and restarted -> remove join nodes from condition and fan-out presentation, connect fall-through branches directly, and give loop completion an explicit labeled node.
proof: Production docs build rendered all five diagrams as static SVG with prompt excerpts and no Mermaid browser runtime. Browser inspection at desktop and a 390x844 emulated viewport showed the implement diagram's prompt-bearing actions and validation/task-limit exits; the mobile expand view kept the complete flow readable. Full prompt disclosures showed exact generated prompt text.
proof: `go test ./internal/generate`, docs `check` and `build`, `just fmt-check`, `just vet`, the full Go suite, all 97 web browser tests on a clean rerun, and `just build` passed. Two consecutive workflow generation runs produced identical hashes for every generated `.mmd` and `.svelte` file.
review: Independent validation found raw-Go fallback labels and CSS shrinking of intrinsic SVG width. Both were repaired: unknown predicates now receive neutral Decision/path labels, and diagram viewports keep intrinsic SVG size, horizontally scroll, and center the workflow spine on load/expand. Revalidation passed with no remaining defect.
