doc_bug: Hosted authoring guidance named the checkout but omitted the paired generated Command and Hosted registration, serving-binary rebuild, and separate state rules -> document the complete supported path and limits.
doc_bug: API examples passed .gimble state directories to NewRuntime and AdmitProject even though both take project directories -> use project paths.
friction: run-prompt help customization removed the standard "Usage of gimble" marker expected by the CLI integration test -> retain that marker while explaining headless ownership.
