correction: Tyler requires generated per-workflow HTTP entrypoints with direct typed calls, matching generated CLI entrypoints; one host may execute many workflows/projects but must not select workflow executables from a runtime name registry.
decision: Assessment only. Preserve the multi-project host and recommend replacing its admission dispatcher; no application edits or live provider runs are part of this assessment.
doc_bug: The API design record sketches workflow-start Forms, but the current remote manifest has none. Treat that sketch as intended design, not existing reuse surface.
decision: Separate shared typed Go admission from shared HTTP wire protocol. Generated CLI JSON endpoints and SKGO browser Forms can use the same operation without copying SvelteKit framing into Gimbal.
friction: Optional workflow parameters already have Polytype devalue support, but current SKGO Form binding bypasses those codecs and cannot bind a scalar into Optional[T]. Target SKGO form binding when implementing start forms; do not assume a Polytype upgrade is required.
doc_bug: Generator tests enforce generic Hosted/Submit wiring, and Gimbal lint still uses Run's pre-model-argument callback index. Green checks do not establish the requested static endpoint architecture.
decision: Current compiled examples interview and implement-interview submit to names absent from the stock server; example execution topology must be explicit when changing generation.
decision: Tyler's follow-up chooses the same generated SKGO remote endpoint for browser and Go CLI clients, replacing the assessment's separate JSON adapter recommendation. The plan includes human start forms and focused SKGO Go-client/Optional-form support.
correction: Workflow definitions are exclusively design/build-time Go. The import cycle is a compile-time package dependency issue; runtime project admission creates state and runs, never definitions.
decision: First-use project admission is part of ordinary workflow submission. Requiring an earlier registration request is an unnecessary restriction; preserve one host and one canonical project owner.
decision: Planning-only follow-up creates implementation-plan.md and definition-of-done.md; no application implementation, SKGO mutation, or live model run is claimed. Completion requires observed shared browser/CLI remotes and overlapping projects in one serving process.
decision: Tyler authorized research-document for missing technical detail followed by the implement workflow. Use isolated Gimbal and SKGO worktrees in one persistent host; research the Kit protocol and generator/package seams before implementing. Workflow role defaults apply to this substantive work; cheap models apply to live behavior demonstrations.
friction: The installed implement workflow deliberately cannot commit, push, merge, or deploy. Its caller must handle the SKGO release between dependency implementation and Gimbal integration, then verify and deliver the completed application.
friction: Research synthesis initially omitted the exact protocol and later misstated first-use discovery as already supported. Focused editorial steering corrected these against local source; implementers must treat the accepted plan as authority and distinguish current behavior from recommendations.
decision: The research handoff supports independent SKGO client work and Gimbal package relocation. Run these as separate implement workflows with disjoint repository ownership, then release SKGO before integrating shared remotes, automatic admission, and visible forms in Gimbal.
correction: The interview example needs a live answer path, not only bare gimbal.Run. A no-question model response cannot prove the example's stated browser question-and-answer purpose; steering restored its existing page composition and an answered live interview was observed by worker and validator.
correction: Moving host state must not leave copied web Runtime wrappers and a second project map. Direct host admission needs to be immediately visible to page/control lookup; the extraction was steered toward one owner and direct caller updates.
friction: One implement validator passed the selected Optional-binding task instead of its larger outcome, ending a run before planned Go-client work. The caller compared code and evidence to the accepted result, preserved the validated change, and submitted the remaining capabilities as separate ordered outcomes. A successful run exit is not architectural completion.
- The commit hook continues its later jobs after a lint failure. Wait for its terminal exit before retrying a commit: overlapping formatter hooks left extra import-group whitespace in four files after the successful checkpoint. `gofmt` restored them exactly to committed content. Do not infer hook completion from the first failed job.
- SKGO client acceptance can advance after transport passes while contract-evolution evidence remains partial. The existing test initially added only a result field; the accepted DoD also requires changed input contracts and rejection of incompatible consumer code. Carried that bounded requirement into the next outcome's planner, which retained it as a distinct required task.
- Browser checks against a worker-owned example server can collide with that worker shutting it down. A submit after validation produced a 500 while port 8080 disappeared; the complete sequence passed on a caller-owned isolated port. Reserve an independent serving process for direct observation rather than sharing a worker's disposable test server.
- User correction: delegate to lower models and keep the caller out of routine implementation/checking. Remaining implement roles were explicitly set to gpt-6-luna:medium, and release follow-through moved to a separate Luna session. Existing workflow defaults had retained expensive Astra/Sol roles; do not mistake workflow delegation alone for satisfying the user's cost preference.
