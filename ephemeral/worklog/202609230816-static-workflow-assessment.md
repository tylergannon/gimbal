correction: Tyler requires generated per-workflow HTTP entrypoints with direct typed calls, matching generated CLI entrypoints; one host may execute many workflows/projects but must not select workflow executables from a runtime name registry.
decision: Assessment only. Preserve the multi-project host and recommend replacing its admission dispatcher; no application edits or live provider runs are part of this assessment.
doc_bug: The API design record sketches workflow-start Forms, but the current remote manifest has none. Treat that sketch as intended design, not existing reuse surface.
decision: Separate shared typed Go admission from shared HTTP wire protocol. Generated CLI JSON endpoints and SKGO browser Forms can use the same operation without copying SvelteKit framing into Gimble.
friction: Optional workflow parameters already have Polytype devalue support, but current SKGO Form binding bypasses those codecs and cannot bind a scalar into Optional[T]. Target SKGO form binding when implementing start forms; do not assume a Polytype upgrade is required.
doc_bug: Generator tests enforce generic Hosted/Submit wiring, and Gimble lint still uses Run's pre-model-argument callback index. Green checks do not establish the requested static endpoint architecture.
decision: Current compiled examples interview and implement-interview submit to names absent from the stock server; example execution topology must be explicit when changing generation.
decision: Tyler's follow-up chooses the same generated SKGO remote endpoint for browser and Go CLI clients, replacing the assessment's separate JSON adapter recommendation. The plan includes human start forms and focused SKGO Go-client/Optional-form support.
correction: Workflow definitions are exclusively design/build-time Go. The import cycle is a compile-time package dependency issue; runtime project admission creates state and runs, never definitions.
decision: First-use project admission is part of ordinary workflow submission. Requiring an earlier registration request is an unnecessary restriction; preserve one host and one canonical project owner.
decision: Planning-only follow-up creates implementation-plan.md and definition-of-done.md; no application implementation, SKGO mutation, or live model run is claimed. Completion requires observed shared browser/CLI remotes and overlapping projects in one serving process.
decision: Tyler authorized research-document for missing technical detail followed by the implement workflow. Use isolated Gimble and SKGO worktrees in one persistent host; research the Kit protocol and generator/package seams before implementing. Workflow role defaults apply to this substantive work; cheap models apply to live behavior demonstrations.
friction: The installed implement workflow deliberately cannot commit, push, merge, or deploy. Its caller must handle the SKGO release between dependency implementation and Gimble integration, then verify and deliver the completed application.
friction: Research synthesis initially omitted the exact protocol and later misstated first-use discovery as already supported. Focused editorial steering corrected these against local source; implementers must treat the accepted plan as authority and distinguish current behavior from recommendations.
decision: The research handoff supports independent SKGO client work and Gimble package relocation. Run these as separate implement workflows with disjoint repository ownership, then release SKGO before integrating shared remotes, automatic admission, and visible forms in Gimble.
