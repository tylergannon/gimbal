correction: Tyler clarified that workflow execution must survive inconsistent provider event streams; missing observation data is a bug, but an event projection error must not crash a progressing role turn.
decision: Claude raw-message projection errors now produce a diagnostic when possible and never return an observer error to the SDK. The exact unknown tool_use_id provider error retains one bounded role-turn retry.
friction: Running just build concurrently with just test produced a transient skgo remote-hash mismatch; an isolated just build passed. Run generation and tests sequentially.
