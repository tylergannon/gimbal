# Jev readiness

decision: Use the unofficial `github.com/kazz187/jev-sdk-go` v0.2.0 for a first Go experiment because its typed batch handles fit Gimble's Go 1.27 code; add it to `go.mod` only with a real consumer. Seven Go SDKs passed local tests and the chosen client completed one live TypeSafe request.

correction: The prior Jev technical note assumed no suitable Go SDK and asserted speculative supervision savings and fixed confidence thresholds. Current evidence supports neither assertion. Re-evaluate SDK and model versions against live docs, and calibrate thresholds on independently labeled Gimble decision points.

decision: Preserve Gimble's runtime-owned supervisor transcript and steer lifecycle. Evaluate frequent Jev judgments as a screen for generative review, beginning with offline replay and live shadowing rather than treating Jev probabilities as authority to steer.

friction: The new `research-document` run completed but its accepted guide asserted that direct HTTP was categorically superior and used uncalibrated fixed thresholds. The `pyramid-summary` run then failed editorial review because its reviewer treated that generated index as authority and called seven real SDK repositories plus an observed live TypeSafe call fabricated. Keep the run status honest, verify claims against primary sources and direct observations, and use the official-docs index as the trustworthy retrieval entry point.

friction: The research workflow wrote a `.go` clip under `ephemeral/`, contrary to this repository's notes-only rule. Remove generated code artifacts before committing research material.

friction: The commit hook rejected a copied 110-page vendor documentation mirror as an oversized ordinary commit. Keep the semantic summaries and saved documentation catalog, link leaves to official pages, and avoid committing the copied corpus.
