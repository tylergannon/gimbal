decision: The docs site keeps the compressed evaluator MP4 as a static asset because artifact uploads have temporary retention. Uploading the same file proves the CLI accepts it; the embedded copy remains playable after the artifact expires.
friction: A stale ~/.local/bin/ffmpeg and ffprobe precede the working Homebrew binaries on this machine's PATH and fail to load libx264. Replaced the broken ffmpeg with a symlink to the working Homebrew binary, preserving the old executable as ffmpeg.pre-gimble-video-20260924. The workflow now resolves a working encoder by default on this machine.
correction: Tyler judged the initial 4x 15 fps and high compression too fast for human review. Use 2x at 25 fps with better quality so the embedded demonstration remains watchable.
friction: Gimble's commit hook caps each committed file at 1 MiB; the complete, higher-quality 2x recording is about 4.9 MB. Keep the complete processed file in evaluator outputs and on the uploaded artifact URL, and embed a short, high-quality highlight from the same session in the static docs site.
