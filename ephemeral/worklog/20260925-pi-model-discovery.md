correction: Accepting `pi/diffusion/<model-id>` was insufficient for agents because the available IDs were not discoverable from Gimbal's instructions.
decision: Document the Router's authenticated model-list endpoint with a chat-and-tools filter; keep routing independent of models.dev and avoid a separate Gimbal catalog command.
