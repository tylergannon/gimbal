decision: Issue #390's observed waiting value was a valid planner result for an unfinished workflow goal. The adapter-private completion state now describes only the current Generate response; genuine work needed for that response remains waiting.
decision: A live regression uses a real PromiseLoop decision and a negative QA assessment, then verifies a background receipt and a same-session follow-up with a different schema. Scripted envelope tests alone cannot show how Claude interprets the completion contract.
decision: Independent review found the live test's output assertions alone could pass with a foreground command. The Sonnet native transcript showed a Bash call with run_in_background true, a waiting envelope, the matching task notification, a Read of receipt.txt, and a completed envelope before the different-schema follow-up. Report that sequence as observed proof; keep the repeatable test on caller-visible behavior without coupling it to Claude's private transcript store.
