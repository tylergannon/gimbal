decision: Pi role bindings use `pi/diffusion/<model-id>`; the selected Router ID goes into the session's isolated Pi config. The models.dev download remains a price source, not a routing dependency.
decision: Current Router chat models with checked limits get those limits in Pi config. Future explicit IDs remain selectable with Pi's conservative defaults until their metadata is checked.
