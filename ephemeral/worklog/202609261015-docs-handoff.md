decision: Gimbal View now serves Gimbal's documentation at https://gimbal-view.fly.dev/docs/; remove the old docs-site source and keep only GitHub Pages redirect files for existing links.
decision: Built-in workflow generation keeps registering typed graphs in Gimbal but no longer writes Mermaid artifacts into a docs application owned by another repository. The optional generator flags remain available for an explicit Gimbal View docs refresh.
friction: just build changed an unrelated generated client import blank line and the embedded web archive; restore those build-only diffs before the docs handoff PR.
