correction: Active sessions and turns are already represented in the observation store, exposed by snapshot and event routes, and used by the browser to target session steering. Reuse these paths; discovery does not need a new state model.
decision: User requests coaches for planner and other working agents against over-engineering, unsolicited features, gold-plating, and hypothetical edge cases without a reasonable actual failing unit test. PromiseLoop already accepts WithSupervisor as of merged PR #264; use it, do not reimplement it.
friction: A real Luna planner plus Luna coach reached a landed scope correction, but starting a third Codex workflow session coincided with shell execution failing with too many open files. Interrupting only this workflow closed its sessions and restored shell execution. Keep this implementation run to one Codex worker and use Haiku for planning/coaching/validation; do not broaden the steering task into daemon changes.
correction: The control transport is the existing HTTP snapshot and SSE observation.Routes over a Unix socket, with only minimal list and steer handlers; do not add JSON-RPC or a separate watch protocol.
finding: Control run discovery must filter shared project run directories through the runtime's existing live.Runs.InProgress table, so concurrent runtimes advertise only runs they can steer.
friction: macOS rejects long Unix socket paths from nested temporary directories; keep discovery under the project control directory and use a short /tmp socket fallback when the project path exceeds the OS limit.
finding: `gimble runs` scans `.gimble/control/*.json`, contacts each advertised Unix socket over the existing `/control/runs` HTTP handler, drops stale/unreachable instances, and includes the discovery PID on each listed run.
finding: `gimble watch` locates the active owner through `/control/runs`, consumes the existing `/api/runs/<id>/events` SSE stream, prints the snapshot with top-level session IDs, and emits subsequent deltas as JSON events until the stream closes.
skill_issue: gimble-workflows source=.agents/skills/gimble-workflows/SKILL.md severity=bug -> The skill validator rejected its existing unquoted colon in the YAML description. Changed it to a shorter folded description; validation now passes.
correction: The first validator called an in-process HTTP package test separate-process proof. That test establishes handler behavior, not the CLI/process boundary; final acceptance still requires running the actual CLI against a separate workflow process.
friction: A parent assignment update sent directly through Codex turn/steer was acknowledged by the coder, but its Gimble coach continued to enforce the older task and denied the update. Continued with the planner's separate assignments rather than competing steering. Provider-level steering is not a substitute for Gimble's own recorded steering path when supervisors need the same context.
