# Interview build workflow

decision: Build #249 through a small Gimble PromiseLoop, starting with two parallel API reconnaissance nodes. Keep downloaded references untracked and expose their absolute directory through scoped context.
decision: User selected Claude/ChatGPT only: Sonnet/Terra for reconnaissance and scope-only supervision; Opus/Sol for hard planning, implementation, and independent validation. No Gemini.
decision: User explicitly requested supervisors to steer against over-engineering and work beyond the issue. Supervisors remain advisory; requirements and observed behavior determine completion.
correction: PromiseLoop exhaustion and a nil Err do not establish fulfillment. The workflow must preserve explicit overall acceptance and return incomplete when dispatch ends without it.
friction: Frontend reconnaissance produced a long proposed implementation alongside API facts. Keep research packets factual and proposed integration notes brief so they do not become a second specification; an operator steer to the planner landed.
friction: The first Opus assignment prescribed types, data structures, and method names despite the outcome-focused Task contract. Preserve the exact dispatch record, steer the worker that the recipe is advisory, and send the planner an outcome-focused instruction for subsequent decisions.
correction: The initial run incorrectly used Claude 4.6 model IDs without verifying the current generation. User specified Opus 5 and requested a fresh restart. Stop all sessions from that run, verify native model resolution, and restart with explicit current IDs; never infer model freshness from CLI acceptance of an old ID.
correction: Restarted reconnaissance incorrectly treated a redundant session-ID check as required for answer delivery. A run plus unique question ID already identifies one waiter. Removed the invented gap from local research before planning; tests should establish exact routing and stale-ID rejection, not require an unasked extra parameter.
correction: A real Sol turn failed on Gimble's invented single-open-reasoning invariant. User rejected bypassing the defect by switching models: fix Gimble when it blocks development. Unexpected ordering or superseded thinking is not workflow failure; exact thinking-token display is not a completion gate. Timebox remaining investigation and file unresolved display bugs.
friction: Normalized session records omitted the rejected native notification, making the exact trigger unrecoverable. Opt-in raw receive-order capture must precede parsing/routing and remain private and untracked.
decision: Upstream investigation found OpenCode already accepts overlapping reasoning; Gimble introduced the fatal producer rule. Codex retries and cancellation can abandon partial items. Retain item identity, treat display projection anomalies as diagnostics, and preserve actual provider and recording failures.

## Codex stream ordering investigation — 2026-09-17

finding: The fatal “second reasoning/text part opened before the first ended” rule was introduced by Gimble's Codex projector in commit `20287b6` (merged as `4c883bc`). It was not inherited from OpenCode. The adapter's single-open-part booleans and its rejection test encoded our assumption as a requirement on the provider.

evidence: `internal/sessionstate/NOTICE.md` identifies the OpenCode port as commit `c55ee2a8152603f04a409163bd3edf79c425fbd7`. That reducer appends reasoning starts and selects the latest unfinished reasoning part; it does not reject overlap. Our existing `internal/sessionstate/testdata/oracle/overlapping-reasoning.jsonl`, `oracle_test.go`, and `selection_test.go` already exercise overlapping reasoning. The reducer's permissive behavior and our producer's fatal invariant contradicted one another.

finding: Codex's normal stream uses identified items, with `itemId` and `summaryIndex` on reasoning summary updates. Normal core processing is serial, but this is not a guarantee that every started thinking item will eventually complete. Cancellation can exit before `OutputItemDone`. A retry starts a fresh sampling request without synthesizing completion for the previous request's local active item. Partial thinking can therefore be abandoned or superseded within a turn. Multiple summary sections are not distinct reasoning items.

evidence: Research checked the exact upstream tag matching the failed native session, `rust-v0.154.0-alpha.6.2` (`b5bffd3ec4db487e7e3dec59663875b0ef7b72ca`), and compared the installed CLI tag `rust-v0.155.0-alpha.2.6` (`bf6f0a4ec97919bf697cdc532e7b8af4ec482fc6`). No reasoning lifecycle/protocol change was found in that targeted comparison. Relevant upstream sources:

- [Sampling retry loop](https://github.com/openai/codex/blob/rust-v0.154.0-alpha.6.2/codex-rs/core/src/session/turn.rs#L1416-L1526).
- [Request-local active item and cancellation](https://github.com/openai/codex/blob/rust-v0.154.0-alpha.6.2/codex-rs/core/src/session/turn.rs#L2274-L2372).
- [Item starts and completion on OutputItemDone](https://github.com/openai/codex/blob/rust-v0.154.0-alpha.6.2/codex-rs/core/src/session/turn.rs#L2399-L2586).
- [Protocol event mapping](https://github.com/openai/codex/blob/rust-v0.154.0-alpha.6.2/codex-rs/app-server-protocol/src/protocol/event_mapping.rs#L378-L418).
- [App-server forwarding and interrupted-turn handling](https://github.com/openai/codex/blob/rust-v0.154.0-alpha.6.2/codex-rs/app-server/src/bespoke_event_handling.rs#L1096-L1241).

boundary: A terminal `turn/completed` is a cleanup boundary even if individual thinking items never completed. `rawResponse/completed` is not a guarantee that all corresponding item completions have arrived. Arrival order must not become a fatal workflow precondition. OpenCode's latest-part display selection also must not be mistaken for native harness item identity.

unknown: The original rejected notification was not retained. The existing ordered session JSONL contains normalized events only; the projector failed before emitting that notification. Native archived rollouts are higher-level records, not raw app-server notification transcripts. Consequently, we cannot claim whether the original trigger was a duplicate start, overlap, or a missing/misrouted completion. `experimentalRawEvents` exposes completed raw response items/boundaries, not a complete raw notification capture.

decision: Development debug mode records inbound WebSocket frames before parsing or routing, preserving receive sequence and exact text including malformed input. `GIMBLE_CODEX_DEBUG_DIR` selects a local directory; connection-specific JSONL files use private permissions and may contain sensitive data. Current captures are under ignored `tmp/codex-debug/`, not committed. Upstream Codex source was inspected in `/private/tmp/codex-source-6t1V4b`, outside the repository; it is Apache-2.0 and was not vendored.

decision: Keep text/reasoning item identity, make duplicate starts harmless, and tolerate overlapping or late completions. Native final-answer selection must not be overwritten by an older item's late completion. Unexpected display projection shapes are warnings, not agent failures; actual provider errors, cancellation, missing final output, and recording-sink errors remain operational failures.

correction: Tyler explicitly does not require perfect display of every thinking token. Unfinished/superseded thinking is not itself a bug in the agent's work. Timebox unresolved investigation to 10–15 minutes, file remaining display/accounting defects, and continue development rather than turn observability into a blocker.

remaining: A live Sol review completed with raw capture enabled and identified residual response-attribution and mixed-kind display-order issues around buffered/superseded parts. These are separate from the fatal ordering assumption. Regression tests cover non-fatal projection mismatches and protection of the actual final answer; the exact original live trigger has not been reproduced. Do not describe either that trigger or perfect transcript ordering as proven fixed.
decision: Remaining display-attribution concerns are tracked in https://github.com/tylergannon/gimble/issues/260. Adapter checkpoint 23a9047 passed package race tests, repository tests, and commit/push checks; the interview implementation run resumed with raw capture enabled.
correction: A request to record findings was additive, not an instruction to stop. Keep the active objective on delivering issue 249 through the supervised workflow and demonstrating the generated example in the browser; an adapter checkpoint or a worklog update is not completion. The operator explicitly requested and now has an active goal for that outcome.
friction: The generated example exits immediately after returning its transcript. A real browser observed both question rounds and a preserved answer after reload, but missed the final completed status when the server exited. The run recorded its transcript and ended successfully. Track viewer lifetime separately in https://github.com/tylergannon/gimble/issues/261 rather than expanding the interview implementation into server lifecycle redesign.
correction: The selected assignment said to preserve and commit existing interview edits, but the operator explicitly retained checkpoint ownership. Preserve the edits and leave all implementation, generation, and validation changes uncommitted and unpushed.
