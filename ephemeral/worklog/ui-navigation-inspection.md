correction: The user wants the implement workflow's current defaults, with Sol coding and validation and Astra planning, actively monitored through delivery. Do not substitute Luna for substantive work to reduce token cost.
decision: Bundle 2 has one implementation owner because topbar navigation and retained detail selection share the same map/page state. Reassess issue 272 separately against the current cards.
doc_bug: skills/gimbal-runs/SKILL.md still calls review the only current built-in; installed implement help is the authoritative invocation for this task.
friction: A browser observing its own active coding transcript recursively captures earlier browser snapshots. Use targeted locators and compact observations, or a small separate demonstration run, instead of full accessibility dumps of that transcript.
decision: Explicit navigation reveal is separate from ordinary map selection so folding an unselected sheet cannot immediately reopen it.
friction: Topbar controls intentionally hide below 1100px; browser interaction tests need a desktop viewport rather than programmatic clicks on hidden elements.
correction: Watcher rows can identify only the latest matching reviewer turn in the selected scope; the snapshot does not correlate that turn to a particular older worker turn, so the detail pane must not imply that association.
