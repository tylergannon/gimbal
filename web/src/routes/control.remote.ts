// Code generated by skgo. DO NOT EDIT.
import { command } from "$app/server";
import type { CancelRun, ControlAccepted, StopTurn } from "./types";

export type { CancelRun, ControlAccepted, StopTurn };

// Every body throws. These functions are implemented in Go, and skgo
// answers their endpoints itself, so anything that renders in the browser
// is proof the Go handler replied rather than this module.
const unimplemented = (): never => {
  throw new Error("skgo: implemented in Go");
};

export const cancelRun = command("unchecked", (_arg: CancelRun): ControlAccepted =>
  unimplemented(),
);

export const stopTurn = command("unchecked", (_arg: StopTurn): ControlAccepted => unimplemented());
