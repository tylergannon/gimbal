// Code generated by skgo. DO NOT EDIT.
import { form, query } from "$app/server";
import type { Conversation } from "../lib/skgo/conversation/types";
import type { CreateConversation, SendConversationMessage } from "./types";

export type { Conversation, CreateConversation, SendConversationMessage };

// Every body throws. These functions are implemented in Go, and skgo
// answers their endpoints itself, so anything that renders in the browser
// is proof the Go handler replied rather than this module.
const unimplemented = (): never => {
  throw new Error("skgo: implemented in Go");
};

export const createConversation = form("unchecked", (_arg: CreateConversation): Conversation =>
  unimplemented(),
);

export const sendConversationMessage = form(
  "unchecked",
  (_arg: SendConversationMessage): Conversation => unimplemented(),
);

export const watchConversation = query.live(
  "unchecked",
  (_arg: string): AsyncIterable<Conversation> => unimplemented(),
);
